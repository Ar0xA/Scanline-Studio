<Project Sdk="Microsoft.NET.Sdk">

  <ItemGroup>
    <Folder Include="Models\" />
    <AvaloniaResource Include="Assets\**" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Avalonia" Version="11.3.12" />
    <PackageReference Include="Avalonia.Desktop" Version="11.3.12" />
    <PackageReference Include="Avalonia.Diagnostics" Version="11.3.12" Condition="'$(Configuration)' == 'Debug'" />
    <PackageReference Include="Avalonia.Themes.Fluent" Version="11.3.12" />
    <PackageReference Include="CommunityToolkit.Mvvm" Version="8.4.2" />
    <PackageReference Include="Microsoft.Extensions.DependencyInjection.Abstractions" Version="8.0.2" />
    <PackageReference Include="Microsoft.Extensions.Logging.Abstractions" Version="8.0.2" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\ScanlineStudio.Application\ScanlineStudio.Application.csproj" />
    <!-- Not a ScanlineStudio.Core.* project (below ScanlineStudio.Abstractions in the layering, see
         spec/12-settings.md); allowed directly, per UiLayeringArchitectureTests's own ban list
         (Core.* projects and Sqlite/ImageSharp packages only). Needed so ScanlineStudio.UI can own
         its own UI-preference settings sections (TxPaneUiSettings etc.) via ISettingsStore. -->
    <ProjectReference Include="..\ScanlineStudio.Settings\ScanlineStudio.Settings.csproj" />
  </ItemGroup>

</Project>
