<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:loc="using:ScanlineStudio.UI.Localization"
        xmlns:vm="using:ScanlineStudio.UI.ViewModels"
        xmlns:sys="using:System.Globalization"
        x:Class="ScanlineStudio.UI.Views.OptionsWindowView"
        x:DataType="vm:OptionsWindowViewModel"
        Title="{loc:Translate Options.Title}"
        Width="820" Height="580"
        MinWidth="660" MinHeight="420"
        WindowStartupLocation="CenterOwner">

    <!-- Real-window verification finding (Phase 2 font-size presets): at Large scale the 10-tab
         strip (General/Appearance/Audio/Radio·CAT/Station/Decode/Identification/Advanced/QRZ.com/
         Forwarding) no longer fit the old Width="720"/MinWidth="560" on one row — "Forwarding"
         wrapped to its own second line. Widened both (was 720/560) to give the wider Large TabItems
         room; still user-resizable narrower if they want (a TabControl wrapping gracefully at a
         width the user chose themselves is expected behavior, not a bug — the bug was starting up
         already wrapped at the window's own default size). -->

    <!-- Industry design port (pre-Phase-7): pure 1:1 restyle of the old "card"/"cardInner" chrome
         onto the established Industry atom set (Atoms.axaml) - no ViewModel/binding changes.
         Priority for this pass is containment (nothing clipped, everything visible) over pixel
         fidelity - no mockup exists for this window, same "extrapolate from the atom set" situation
         as Phase 6's Logbook tab. Sections that already had their own heading text become a real
         IndustryGroupBoxTheme with that text as the Header; sections that never had one stay a
         plain IndustryBlueprintBorderTheme box (no invented header copy). Every RadioButton group -
         both the 3 real ones (CAT backend) and every disabled placeholder group - uses
         IndustryMiniRadioTheme inside a WrapPanel (not a plain horizontal StackPanel), so a narrow
         resize wraps chips to a new line instead of clipping them; same WrapPanel-plus-per-item-
         margin idiom this file's own color-swatch row already used before this port. Window
         Width/MinWidth were widened from the old 640/480 - 9 tab headers (as of the Forwarding tab,
         2026-08-15; incl. "Radio / CAT" and "Identification") plus the Advanced tab's 4-column
         stepper grid made the old floor a real clipping risk; verify via a real-window screenshot at
         both default size and the MinWidth floor, not assumed safe from arithmetic alone. -->

    <Border Background="{DynamicResource IndustryBg}">
        <Grid RowDefinitions="*,Auto">

            <TabControl Grid.Row="0" Classes="Industry" SelectedIndex="{Binding SelectedTabIndex, Mode=TwoWay}">

                <TabItem Classes="Industry" Header="{loc:Translate Options.General.Tab}">
                    <ScrollViewer>
                        <StackPanel Spacing="{StaticResource IndustrySpacingGroup}" Margin="{StaticResource IndustryPaddingGroup}">
                            <!-- RememberWindowPosition (window-geometry persistence) and JpegQuality
                                 (export quality) are both real, bound settings — see this ContentControl's
                                 contents below. -->
                            <ContentControl Classes="Industry" Theme="{StaticResource IndustryBlueprintBorderTheme}" Padding="{StaticResource IndustryGbPadding}">
                                <StackPanel Spacing="{StaticResource IndustrySpacingSnug}">
                                    <TextBlock Classes="IndustryRowLabel" Text="{loc:Translate Options.General.Language}" />
                                    <ComboBox Classes="Industry IndustryInput" ItemsSource="{Binding AvailableCultures}"
                                              SelectedItem="{Binding SelectedCulture}"
                                              HorizontalAlignment="Stretch">
                                        <ComboBox.ItemTemplate>
                                            <DataTemplate x:DataType="sys:CultureInfo">
                                                <TextBlock Text="{Binding DisplayName}" />
                                            </DataTemplate>
                                        </ComboBox.ItemTemplate>
                                    </ComboBox>

                                    <CheckBox Classes="Industry" Theme="{StaticResource IndustryCheckBoxTheme}"
                                              Content="{loc:Translate Options.General.RememberWindow}"
                                              IsChecked="{Binding RememberWindowPosition}" />
                                    <Grid ColumnDefinitions="Auto,Auto,*">
                                        <TextBlock Classes="IndustryRowLabel" VerticalAlignment="Center" Text="{loc:Translate Options.General.JpegQuality}" />
                                        <TextBlock Grid.Column="1" Classes="IndustryHelpGlyph" Margin="{StaticResource IndustryPaddingHairline}" Text="{loc:Translate Options.HelpGlyph}" ToolTip.Tip="{loc:Translate Options.General.JpegQuality.Help}" />
                                        <NumericUpDown Grid.Column="2" Classes="Industry" Theme="{StaticResource IndustryStepperTheme}" Value="{Binding JpegQuality}" Minimum="1" Maximum="100" HorizontalAlignment="Left" />
                                    </Grid>
                                </StackPanel>
                            </ContentControl>

                            <!-- Storage section: 4 rows, moved in from the former standalone
                                 "Configurations > Storage" dialog (Images) plus 3 new rows
                                 (Config/Database/Log). Each row commits (Images/Config/Log,
                                 immediately) or stages (Database only, restart-required by standing
                                 user decision) on its OWN Apply — none of these 4 rows are staged
                                 behind this dialog's own Save/Cancel/Reset flow below. -->
                            <HeaderedContentControl Classes="Industry" Theme="{StaticResource IndustryGroupBoxTheme}">
                                <HeaderedContentControl.Header>
                                    <TextBlock Classes="IndustryGbTitle" Text="{loc:Translate Options.General.Storage.Header}" />
                                </HeaderedContentControl.Header>
                                <StackPanel Spacing="{StaticResource IndustrySpacingGroup}">

                                    <!-- Images: unchanged behavior from the former standalone dialog. -->
                                    <StackPanel Spacing="{StaticResource IndustrySpacingSnug}">
                                        <TextBlock Classes="IndustryRowLabel" Text="{loc:Translate Options.General.Storage.Images.Label}" />
                                        <Grid ColumnDefinitions="*,Auto" ColumnSpacing="6">
                                            <TextBox Classes="Industry IndustryInput" Text="{Binding ImagesDirectory}" />
                                            <Button Grid.Column="1" Classes="Industry IndustryBtn IndustryBtn24 IndustryBtnSecondary" Padding="7,0"
                                                    Content="{loc:Translate Options.General.Storage.BrowseButton}" Command="{Binding BrowseImagesDirectoryCommand}" />
                                        </Grid>
                                        <TextBlock Classes="IndustryRowLabel" Text="{loc:Translate Options.General.Storage.Images.Caption}" TextWrapping="Wrap" />
                                        <TextBlock Text="{Binding ImagesDirectoryErrorMessage}" Foreground="{DynamicResource IndustryDanger}" TextWrapping="Wrap"
                                                   IsVisible="{Binding ImagesDirectoryErrorMessage, Converter={x:Static ObjectConverters.IsNotNull}}" />
                                        <Button Classes="Industry IndustryBtn IndustryBtn24 IndustryBtnSecondary" Padding="7,0" HorizontalAlignment="Left"
                                                Content="{loc:Translate Options.General.Storage.ApplyButton}" Command="{Binding ApplyImagesDirectoryCommand}" />
                                    </StackPanel>

                                    <!-- Audio: ui_transition_plan.md step 12 (Auto-save RX audio).
                                         Combined enable+directory row, both applied together on one
                                         Apply click (matches IReceiveHistoryStore.SetAudioSettingsAsync's
                                         own combined-arguments contract). -->
                                    <StackPanel Spacing="{StaticResource IndustrySpacingSnug}">
                                        <CheckBox Classes="Industry" Theme="{StaticResource IndustryCheckBoxTheme}"
                                                  Content="{loc:Translate Options.General.Storage.Audio.Label}"
                                                  IsChecked="{Binding AudioSaveEnabled}" />
                                        <Grid ColumnDefinitions="*,Auto" ColumnSpacing="6">
                                            <TextBox Classes="Industry IndustryInput" Text="{Binding AudioDirectory}" />
                                            <Button Grid.Column="1" Classes="Industry IndustryBtn IndustryBtn24 IndustryBtnSecondary" Padding="7,0"
                                                    Content="{loc:Translate Options.General.Storage.BrowseButton}" Command="{Binding BrowseAudioDirectoryCommand}" />
                                        </Grid>
                                        <TextBlock Classes="IndustryRowLabel" Text="{loc:Translate Options.General.Storage.Audio.Caption}" TextWrapping="Wrap" />
                                        <TextBlock Text="{Binding AudioDirectoryErrorMessage}" Foreground="{DynamicResource IndustryDanger}" TextWrapping="Wrap"
                                                   IsVisible="{Binding AudioDirectoryErrorMessage, Converter={x:Static ObjectConverters.IsNotNull}}" />
                                        <Button Classes="Industry IndustryBtn IndustryBtn24 IndustryBtnSecondary" Padding="7,0" HorizontalAlignment="Left"
                                                Content="{loc:Translate Options.General.Storage.ApplyButton}" Command="{Binding ApplyAudioDirectoryCommand}" />
                                    </StackPanel>

                                    <!-- Config: settings.json's directory. Applies live, no restart
                                         needed (restart-required-settings backlog item 3, 2026-08-27)
                                         — same shape as Log below, not Database's staged/Restart-Now
                                         shape any more. -->
                                    <StackPanel Spacing="{StaticResource IndustrySpacingSnug}">
                                        <TextBlock Classes="IndustryRowLabel" Text="{loc:Translate Options.General.Storage.Config.Label}" />
                                        <StackPanel Orientation="Horizontal" Spacing="{StaticResource IndustrySpacingHairline}">
                                            <TextBlock Classes="IndustryRowLabel" Text="{loc:Translate Options.General.Storage.CurrentlyUsingLabel}" />
                                            <TextBlock Classes="IndustryRowLabel" Text="{Binding ConfigDirectory}" TextWrapping="Wrap" />
                                        </StackPanel>
                                        <Grid ColumnDefinitions="*,Auto" ColumnSpacing="6">
                                            <TextBox Classes="Industry IndustryInput" Text="{Binding ConfigDirectoryInput}"
                                                     Watermark="{loc:Translate Options.General.Storage.NewFolderWatermark}" />
                                            <Button Grid.Column="1" Classes="Industry IndustryBtn IndustryBtn24 IndustryBtnSecondary" Padding="7,0"
                                                    Content="{loc:Translate Options.General.Storage.BrowseButton}" Command="{Binding BrowseConfigDirectoryCommand}" />
                                        </Grid>
                                        <TextBlock Classes="IndustryRowLabel" Text="{loc:Translate Options.General.Storage.Config.Caption}" TextWrapping="Wrap" />
                                        <TextBlock Text="{Binding ConfigDirectoryErrorMessage}" Foreground="{DynamicResource IndustryDanger}" TextWrapping="Wrap"
                                                   IsVisible="{Binding ConfigDirectoryErrorMessage, Converter={x:Static ObjectConverters.IsNotNull}}" />
                                        <Button Classes="Industry IndustryBtn IndustryBtn24 IndustryBtnSecondary" Padding="7,0" HorizontalAlignment="Left"
                                                Content="{loc:Translate Options.General.Storage.ApplyButton}" Command="{Binding ApplyConfigDirectoryCommand}" />
                                    </StackPanel>

                                    <!-- Database: history.db's directory (shared by Logbook + Receive
                                         History). Stages a pending target; Apply shows this row's own
                                         Restart-Now/Not-Now confirm — permanently restart-required by
                                         standing user decision (SQLite's pooled native handles make a
                                         live relocation unsafe), unlike Config above, which used to
                                         share this exact shape before restart-required-settings
                                         backlog item 3 (2026-08-27). -->
                                    <StackPanel Spacing="{StaticResource IndustrySpacingSnug}">
                                        <TextBlock Classes="IndustryRowLabel" Text="{loc:Translate Options.General.Storage.Database.Label}" />
                                        <StackPanel Orientation="Horizontal" Spacing="{StaticResource IndustrySpacingHairline}">
                                            <TextBlock Classes="IndustryRowLabel" Text="{loc:Translate Options.General.Storage.CurrentlyUsingLabel}" />
                                            <TextBlock Classes="IndustryRowLabel" Text="{Binding DatabaseDirectory}" TextWrapping="Wrap" />
                                        </StackPanel>
                                        <Grid ColumnDefinitions="*,Auto" ColumnSpacing="6">
                                            <TextBox Classes="Industry IndustryInput" Text="{Binding PendingDatabaseDirectory}"
                                                     Watermark="{loc:Translate Options.General.Storage.NewFolderWatermark}" />
                                            <Button Grid.Column="1" Classes="Industry IndustryBtn IndustryBtn24 IndustryBtnSecondary" Padding="7,0"
                                                    Content="{loc:Translate Options.General.Storage.BrowseButton}" Command="{Binding BrowseDatabaseDirectoryCommand}" />
                                        </Grid>
                                        <TextBlock Classes="IndustryRowLabel" Foreground="{DynamicResource IndustryDanger}" FontWeight="Bold" Text="{loc:Translate Options.General.Storage.RestartRequiredCaption}" TextWrapping="Wrap" />
                                        <TextBlock Text="{Binding DatabaseDirectoryErrorMessage}" Foreground="{DynamicResource IndustryDanger}" TextWrapping="Wrap"
                                                   IsVisible="{Binding DatabaseDirectoryErrorMessage, Converter={x:Static ObjectConverters.IsNotNull}}" />
                                        <Button Classes="Industry IndustryBtn IndustryBtn24 IndustryBtnSecondary" Padding="7,0" HorizontalAlignment="Left"
                                                Content="{loc:Translate Options.General.Storage.ApplyButton}" Command="{Binding ApplyDatabaseDirectoryCommand}"
                                                IsVisible="{Binding !IsConfirmingDatabaseRestart}" />
                                        <StackPanel Orientation="Horizontal" Spacing="{StaticResource IndustrySpacingSnug}" IsVisible="{Binding IsConfirmingDatabaseRestart}">
                                            <TextBlock Classes="IndustryRowLabel" Text="{loc:Translate Options.General.Storage.RestartConfirmPrompt}" TextWrapping="Wrap" MaxWidth="300" VerticalAlignment="Center" />
                                            <Button Classes="Industry IndustryBtn IndustryBtn24 IndustryBtnPrimary" Padding="7,0"
                                                    Content="{loc:Translate Options.General.Storage.RestartNowButton}" Command="{Binding ConfirmDatabaseRestartCommand}" />
                                            <Button Classes="Industry IndustryBtn IndustryBtn24 IndustryBtnSecondary" Padding="7,0"
                                                    Content="{loc:Translate Options.General.Storage.NotNowButton}" Command="{Binding CancelDatabaseRestartCommand}" />
                                        </StackPanel>
                                    </StackPanel>

                                    <!-- Log: applies live, no restart needed. -->
                                    <StackPanel Spacing="{StaticResource IndustrySpacingSnug}">
                                        <TextBlock Classes="IndustryRowLabel" Text="{loc:Translate Options.General.Storage.Log.Label}" />
                                        <Grid ColumnDefinitions="*,Auto" ColumnSpacing="6">
                                            <TextBox Classes="Industry IndustryInput" Text="{Binding LogDirectory}" />
                                            <Button Grid.Column="1" Classes="Industry IndustryBtn IndustryBtn24 IndustryBtnSecondary" Padding="7,0"
                                                    Content="{loc:Translate Options.General.Storage.BrowseButton}" Command="{Binding BrowseLogDirectoryCommand}" />
                                        </Grid>
                                        <TextBlock Classes="IndustryRowLabel" Text="{loc:Translate Options.General.Storage.Log.Caption}" TextWrapping="Wrap" />
                                        <TextBlock Text="{Binding LogDirectoryErrorMessage}" Foreground="{DynamicResource IndustryDanger}" TextWrapping="Wrap"
                                                   IsVisible="{Binding LogDirectoryErrorMessage, Converter={x:Static ObjectConverters.IsNotNull}}" />
                                        <Button Classes="Industry IndustryBtn IndustryBtn24 IndustryBtnSecondary" Padding="7,0" HorizontalAlignment="Left"
                                                Content="{loc:Translate Options.General.Storage.ApplyButton}" Command="{Binding ApplyLogDirectoryCommand}" />
                                    </StackPanel>

                                </StackPanel>
                            </HeaderedContentControl>

                            <!-- Options stub backlog item 6 (7 per-element waterfall/spectrum
                                 colors): REMOVED 2026-08-28, not wired — this port already has a
                                 deliberately-designed, already-shipped fixed palette
                                 (WaterfallPalette.cs's SDR-style heatmap gradient,
                                 SpectrumTraceControl.cs's fixed trace/marker pens) replacing
                                 legacy's raw per-element user-editable colors, per spec/09-ui.md's
                                 own explicit exemption of this visualization from strict legacy-port
                                 fidelity. Exposing 7 raw color pickers would let a user override that
                                 already-shipped cohesive design, not fill a real gap — see
                                 docs/removed-features.md's own entry for the full reasoning. -->

                            <Button Classes="Industry IndustryBtn IndustryBtn24 IndustryBtnSecondary" Padding="7,0" Content="{loc:Translate Options.ResetSection}" Command="{Binding ResetGeneralToDefaultCommand}" />
                        </StackPanel>
                    </ScrollViewer>
                </TabItem>

                <TabItem Classes="Industry" Header="{loc:Translate Options.Appearance.Tab}">
                    <ScrollViewer>
                        <StackPanel Spacing="{StaticResource IndustrySpacingGroup}" Margin="{StaticResource IndustryPaddingGroup}">
                            <!-- Phase 1 dark mode: 3-way theme radio group, same RadioButton-group +
                                 IsXSelected boolean-projection idiom as the Decode tab's SenseLevel
                                 group. Applies live (no restart, no dialog close/reopen needed) — see
                                 OptionsWindowViewModel.SaveCoreUnguardedAsync's own live-apply block. -->
                            <ContentControl Classes="Industry" Theme="{StaticResource IndustryBlueprintBorderTheme}" Padding="{StaticResource IndustryGbPadding}">
                                <StackPanel Spacing="{StaticResource IndustrySpacingSnug}">
                                    <TextBlock Classes="IndustryRowLabel" Text="{loc:Translate Options.Appearance.Theme}" />
                                    <WrapPanel>
                                        <RadioButton Classes="Industry IndustryMini" Theme="{StaticResource IndustryMiniRadioTheme}" Margin="0,0,4,4" GroupName="AppTheme" Content="{loc:Translate Options.Appearance.Theme.Light}" IsChecked="{Binding IsAppThemeLightSelected}" />
                                        <RadioButton Classes="Industry IndustryMini" Theme="{StaticResource IndustryMiniRadioTheme}" Margin="0,0,4,4" GroupName="AppTheme" Content="{loc:Translate Options.Appearance.Theme.Dark}" IsChecked="{Binding IsAppThemeDarkSelected}" />
                                        <RadioButton Classes="Industry IndustryMini" Theme="{StaticResource IndustryMiniRadioTheme}" Margin="0,0,4,4" GroupName="AppTheme" Content="{loc:Translate Options.Appearance.Theme.System}" IsChecked="{Binding IsAppThemeSystemSelected}" />
                                    </WrapPanel>
                                </StackPanel>
                            </ContentControl>

                            <!-- Phase 2 font-size presets: same RadioButton-group + IsXSelected
                                 idiom as the theme group above, sharing this tab's own Reset
                                 button. Applies live on Save — see
                                 OptionsWindowViewModel.SaveCoreUnguardedAsync's own separate
                                 live-apply block for FontScale. -->
                            <ContentControl Classes="Industry" Theme="{StaticResource IndustryBlueprintBorderTheme}" Padding="{StaticResource IndustryGbPadding}">
                                <StackPanel Spacing="{StaticResource IndustrySpacingSnug}">
                                    <TextBlock Classes="IndustryRowLabel" Text="{loc:Translate Options.Appearance.FontScale}" />
                                    <WrapPanel>
                                        <RadioButton Classes="Industry IndustryMini" Theme="{StaticResource IndustryMiniRadioTheme}" Margin="0,0,4,4" GroupName="FontScale" Content="{loc:Translate Options.Appearance.FontScale.Normal}" IsChecked="{Binding IsFontScaleNormalSelected}" />
                                        <RadioButton Classes="Industry IndustryMini" Theme="{StaticResource IndustryMiniRadioTheme}" Margin="0,0,4,4" GroupName="FontScale" Content="{loc:Translate Options.Appearance.FontScale.Large}" IsChecked="{Binding IsFontScaleLargeSelected}" />
                                    </WrapPanel>
                                </StackPanel>
                            </ContentControl>

                            <Button Classes="Industry IndustryBtn IndustryBtn24 IndustryBtnSecondary" Padding="7,0" Content="{loc:Translate Options.ResetSection}" Command="{Binding ResetAppearanceToDefaultCommand}" />
                        </StackPanel>
                    </ScrollViewer>
                </TabItem>

                <TabItem Classes="Industry" Header="{loc:Translate Options.Audio.Tab}">
                    <ScrollViewer>
                        <StackPanel Spacing="{StaticResource IndustrySpacingGroup}" Margin="{StaticResource IndustryPaddingGroup}">
                            <ContentControl Classes="Industry" Theme="{StaticResource IndustryBlueprintBorderTheme}" Padding="{StaticResource IndustryGbPadding}">
                                <StackPanel Spacing="{StaticResource IndustrySpacingSnug}">
                                    <TextBlock Classes="IndustryRowLabel" Text="{loc:Translate Options.Audio.CaptureDevice}" />
                                    <ComboBox Classes="Industry IndustryInput" ItemsSource="{Binding CaptureDevices}"
                                              SelectedItem="{Binding SelectedCaptureDevice}"
                                              HorizontalAlignment="Stretch">
                                        <ComboBox.ItemTemplate>
                                            <DataTemplate>
                                                <TextBlock Text="{Binding Name}" />
                                            </DataTemplate>
                                        </ComboBox.ItemTemplate>
                                    </ComboBox>

                                    <TextBlock Classes="IndustryRowLabel" Text="{loc:Translate Options.Audio.PlaybackDevice}" />
                                    <ComboBox Classes="Industry IndustryInput" ItemsSource="{Binding PlaybackDevices}"
                                              SelectedItem="{Binding SelectedPlaybackDevice}"
                                              HorizontalAlignment="Stretch">
                                        <ComboBox.ItemTemplate>
                                            <DataTemplate>
                                                <TextBlock Text="{Binding Name}" />
                                            </DataTemplate>
                                        </ComboBox.ItemTemplate>
                                    </ComboBox>

                                    <Grid ColumnDefinitions="Auto,Auto,*">
                                        <TextBlock Classes="IndustryRowLabel" VerticalAlignment="Center" Text="{loc:Translate Options.Audio.SampleRate}" />
                                        <TextBlock Grid.Column="1" Classes="IndustryHelpGlyph" Margin="{StaticResource IndustryPaddingHairline}" Text="{loc:Translate Options.HelpGlyph}" ToolTip.Tip="{loc:Translate Options.Audio.SampleRate.Help}" />
                                        <!-- Legacy's real EditSamp control (Option.h:97) is a TComboBox
                                             with a static preset list, still editable — see
                                             OptionsWindowViewModel.AvailableSampleRates' own doc comment. -->
                                        <ComboBox Grid.Column="2" Classes="Industry IndustryInput" ItemsSource="{Binding AvailableSampleRates}"
                                                  Text="{Binding SampleRate}" IsEditable="True" />
                                    </Grid>

                                    <!-- Stub survey Tier 3, "Clock calibration" piece 1 (2026-08-26):
                                         placed next to Sample rate, matching legacy's own placement
                                         (Option.h TxSampOff/UDTxSamp sit beside EditSamp on legacy's
                                         Options dialog — round-2 plan-review finding, this port has
                                         no separate Calibration-menu equivalent to this specific
                                         control). Increment="0.02" matches legacy's own spinner step
                                         (NormalSampFreq(x,50) quantization); Minimum/Maximum match
                                         legacy's own accepted +/-1500Hz manual range (Option.cpp:1142). -->
                                    <Grid ColumnDefinitions="Auto,Auto,*">
                                        <TextBlock Classes="IndustryRowLabel" VerticalAlignment="Center" Text="{loc:Translate Options.Audio.TxSampleRateOffset}" />
                                        <TextBlock Grid.Column="1" Classes="IndustryHelpGlyph" Margin="{StaticResource IndustryPaddingHairline}" Text="{loc:Translate Options.HelpGlyph}" ToolTip.Tip="{loc:Translate Options.Audio.TxSampleRateOffset.Help}" />
                                        <NumericUpDown Grid.Column="2" Classes="Industry" Theme="{StaticResource IndustryStepperTheme}" Value="{Binding TxSampleRateOffsetHz}" Increment="0.02" Minimum="-1500" Maximum="1500" FormatString="0.00" HorizontalAlignment="Left" />
                                    </Grid>
                                </StackPanel>
                            </ContentControl>

                            <!-- Wired 2026-08-12: Source/stereo (AudioDeviceSettings.CaptureChannelSource),
                                 CBSTX/StereoTx (AudioDeviceSettings.StereoTxEnabled): both had a real,
                                 already wired backend with zero UI path.
                                 RxFifo/TxFifo/SoundPriority/AppPriority removed 2026-08-27 (Options
                                 restart-required audit, "no significant modern benefit" finding):
                                 RxFifo/TxFifo's legacy Win32 waveIn/waveOut buffer-count concept has no
                                 analog in this port's MiniAudio-based engine, superseded by its own
                                 automatic buffering; SoundPriority was a dead control even in legacy
                                 itself (m_SoundPriority was read/persisted but never applied to
                                 anything); AppPriority (Process.PriorityClass) worked but real
                                 anti-glitch scheduling on a modern OS happens elsewhere (Windows MMCSS,
                                 Linux PipeWire/JACK RT threads, macOS Core Audio thread policies), not
                                 whole-process priority class — see docs/removed-features.md. -->
                            <HeaderedContentControl Classes="Industry" Theme="{StaticResource IndustryGroupBoxTheme}">
                                <HeaderedContentControl.Header>
                                    <TextBlock Classes="IndustryGbTitle" Text="{loc:Translate Options.Audio.PerformanceHeader}" />
                                </HeaderedContentControl.Header>
                                <StackPanel Spacing="{StaticResource IndustrySpacingSnug}">
                                    <StackPanel Orientation="Horizontal" Spacing="{StaticResource IndustrySpacingTight}">
                                        <TextBlock Classes="IndustryRowLabel" VerticalAlignment="Center" Text="{loc:Translate Options.Audio.StereoSource}" />
                                        <TextBlock Classes="IndustryHelpGlyph" Text="{loc:Translate Options.HelpGlyph}" ToolTip.Tip="{loc:Translate Options.Audio.StereoSource.Help}" />
                                    </StackPanel>
                                    <WrapPanel>
                                        <RadioButton Classes="Industry IndustryMini" Theme="{StaticResource IndustryMiniRadioTheme}" Margin="0,0,4,4" GroupName="StereoSource" Content="{loc:Translate Options.Audio.StereoSource.Mono}" IsChecked="{Binding IsCaptureChannelMonoSelected}" />
                                        <RadioButton Classes="Industry IndustryMini" Theme="{StaticResource IndustryMiniRadioTheme}" Margin="0,0,4,4" GroupName="StereoSource" Content="{loc:Translate Options.Audio.StereoSource.Left}" IsChecked="{Binding IsCaptureChannelLeftSelected}" />
                                        <RadioButton Classes="Industry IndustryMini" Theme="{StaticResource IndustryMiniRadioTheme}" Margin="0,0,4,4" GroupName="StereoSource" Content="{loc:Translate Options.Audio.StereoSource.Right}" IsChecked="{Binding IsCaptureChannelRightSelected}" />
                                    </WrapPanel>
                                    <CheckBox Classes="Industry" Theme="{StaticResource IndustryCheckBoxTheme}" Content="{loc:Translate Options.Audio.StereoTx}"
                                              IsChecked="{Binding StereoTxEnabled}" />
                                </StackPanel>
                            </HeaderedContentControl>

                            <Button Classes="Industry IndustryBtn IndustryBtn24 IndustryBtnSecondary" Padding="7,0" Content="{loc:Translate Options.ResetSection}" Command="{Binding ResetAudioToDefaultCommand}" />
                        </StackPanel>
                    </ScrollViewer>
                </TabItem>

                <TabItem Classes="Industry" Header="{loc:Translate Options.Radio.Tab}">
                    <ScrollViewer>
                        <StackPanel Spacing="{StaticResource IndustrySpacingGroup}" Margin="{StaticResource IndustryPaddingGroup}">
                            <ContentControl Classes="Industry" Theme="{StaticResource IndustryBlueprintBorderTheme}" Padding="{StaticResource IndustryGbPadding}">
                                <StackPanel Spacing="{StaticResource IndustrySpacingSnug}">
                                    <StackPanel Orientation="Horizontal" Spacing="{StaticResource IndustrySpacingTight}">
                                        <TextBlock Classes="IndustryRowLabel" VerticalAlignment="Center" Text="{loc:Translate Options.Radio.Backend}" />
                                        <TextBlock Classes="IndustryHelpGlyph" Text="{loc:Translate Options.HelpGlyph}" ToolTip.Tip="{loc:Translate Options.Radio.Backend.Help}" />
                                    </StackPanel>
                                    <WrapPanel>
                                        <RadioButton Classes="Industry IndustryMini" Theme="{StaticResource IndustryMiniRadioTheme}" Margin="0,0,4,4"
                                                     GroupName="RadioBackend"
                                                     Content="{loc:Translate Options.Radio.Backend.None}"
                                                     IsChecked="{Binding IsNoneBackendSelected}" />
                                        <RadioButton Classes="Industry IndustryMini" Theme="{StaticResource IndustryMiniRadioTheme}" Margin="0,0,4,4"
                                                     GroupName="RadioBackend"
                                                     Content="{loc:Translate Options.Radio.Backend.Rigctld}"
                                                     IsChecked="{Binding IsRigctldBackendSelected}" />
                                        <RadioButton Classes="Industry IndustryMini" Theme="{StaticResource IndustryMiniRadioTheme}" Margin="0,0,4,4"
                                                     GroupName="RadioBackend"
                                                     Content="{loc:Translate Options.Radio.Backend.Hamlib}"
                                                     IsChecked="{Binding IsHamlibBackendSelected}" />
                                        <RadioButton Classes="Industry IndustryMini" Theme="{StaticResource IndustryMiniRadioTheme}" Margin="0,0,4,4"
                                                     GroupName="RadioBackend"
                                                     Content="{loc:Translate Options.Radio.Backend.FlRig}"
                                                     IsChecked="{Binding IsFlrigBackendSelected}" />
                                        <!-- OmniRig is Windows-only (a COM automation server) —
                                             IsEnabled gates on IsOmniRigBackendAvailable so a
                                             non-Windows user can't select a backend that will only
                                             ever fail at Connect time (OmniRigProtocolFactory.Create
                                             throws PlatformNotSupportedException). -->
                                        <RadioButton Classes="Industry IndustryMini" Theme="{StaticResource IndustryMiniRadioTheme}" Margin="0,0,4,4"
                                                     GroupName="RadioBackend"
                                                     Content="{loc:Translate Options.Radio.Backend.OmniRig}"
                                                     ToolTip.Tip="{Binding OmniRigBackendTooltip}"
                                                     ToolTip.ShowOnDisabled="True"
                                                     IsEnabled="{Binding IsOmniRigBackendAvailable}"
                                                     IsChecked="{Binding IsOmniRigBackendSelected}" />
                                    </WrapPanel>

                                    <!-- User-reported gap: the real session only ever connected at
                                         app startup — Save alone needed a full restart before the
                                         main window showed anything connected, and Test CAT/PTT's own
                                         "disconnect first" guard had no way to be satisfied. Applies
                                         regardless of which backend section below is expanded, since
                                         it acts on whatever backend is ACTUALLY connected right now,
                                         not necessarily the one currently shown/being edited. -->
                                    <Grid ColumnDefinitions="Auto,*">
                                        <!-- User-reported gap: Connect was enabled unconditionally —
                                             including for "None" (nothing to connect to) and for
                                             rigctld/Hamlib configs never actually verified to work.
                                             IsEnabled gates on CanToggleRadioConnection (Connect
                                             requires a successful test first; Disconnect never does).
                                             ToolTip.ShowOnDisabled="True" — Avalonia doesn't show
                                             tooltips on a disabled control by default, so without
                                             this a disabled button would just sit there unexplained. -->
                                        <Button Classes="Industry IndustryBtn IndustryBtn24 IndustryBtnSecondary"
                                                Content="{Binding ConnectRadioButtonLabel}"
                                                Command="{Binding ToggleRadioConnectionCommand}"
                                                IsEnabled="{Binding CanToggleRadioConnection}"
                                                ToolTip.Tip="{Binding ConnectRadioTooltip}"
                                                ToolTip.ShowOnDisabled="True" />
                                        <TextBlock Grid.Column="1" Classes="IndustryRowValue" VerticalAlignment="Center" Margin="8,0,0,0"
                                                   TextWrapping="Wrap"
                                                   Text="{Binding ConnectRadioErrorMessage}"
                                                   IsVisible="{Binding ConnectRadioErrorMessage, Converter={x:Static ObjectConverters.IsNotNull}}" />
                                    </Grid>
                                    <!-- Purely presentational — see RadioLinkStatusMessage's own doc
                                         comment. Never touches the button above (label/enablement/
                                         action are untouched). -->
                                    <TextBlock Classes="IndustryRowValue" TextWrapping="Wrap"
                                               Text="{Binding RadioLinkStatusMessage}"
                                               IsVisible="{Binding RadioLinkStatusMessage, Converter={x:Static ObjectConverters.IsNotNull}}" />

                                    <StackPanel IsVisible="{Binding IsRigctldSelected}" Spacing="{StaticResource IndustrySpacingSnug}">
                                        <Grid ColumnDefinitions="Auto,*">
                                            <TextBlock Classes="IndustryRowLabel" VerticalAlignment="Center" Text="{loc:Translate Options.Radio.Host}" />
                                            <TextBox Grid.Column="1" Classes="Industry IndustryInput" Text="{Binding RigctldHost}" />
                                        </Grid>
                                        <Grid ColumnDefinitions="Auto,*">
                                            <TextBlock Classes="IndustryRowLabel" VerticalAlignment="Center" Text="{loc:Translate Options.Radio.Port}" />
                                            <NumericUpDown Grid.Column="1" Classes="Industry" Theme="{StaticResource IndustryStepperTheme}" Value="{Binding RigctldPort}" HorizontalAlignment="Left" />
                                        </Grid>
                                        <!-- Auditor usability review follow-up (2026-08-18): tests
                                             whatever is CURRENTLY TYPED into Host/Port above (not
                                             necessarily saved yet) via a fresh, disposable connection
                                             attempt — never disturbs the app's own real, persistent
                                             radio session (TestRigctldConnectionCommand's own doc
                                             comment). -->
                                        <Grid ColumnDefinitions="Auto,*">
                                            <Button Classes="Industry IndustryBtn IndustryBtn24 IndustryBtnSecondary"
                                                    Content="{loc:Translate Options.Radio.TestConnection}"
                                                    Command="{Binding TestRigctldConnectionCommand}" />
                                            <TextBlock Grid.Column="1" Classes="IndustryRowValue" VerticalAlignment="Center" Margin="8,0,0,0"
                                                       TextWrapping="Wrap"
                                                       Text="{Binding TestConnectionStatusMessage}"
                                                       IsVisible="{Binding TestConnectionStatusMessage, Converter={x:Static ObjectConverters.IsNotNull}}" />
                                        </Grid>
                                    </StackPanel>

                                    <StackPanel IsVisible="{Binding IsHamlibSelected}" Spacing="{StaticResource IndustrySpacingSnug}">
                                        <Grid ColumnDefinitions="Auto,Auto,*">
                                            <TextBlock Classes="IndustryRowLabel" VerticalAlignment="Center" Text="{loc:Translate Options.Radio.Hamlib.LibraryPath}" />
                                            <TextBlock Grid.Column="1" Classes="IndustryHelpGlyph" Margin="{StaticResource IndustryPaddingHairline}" Text="{loc:Translate Options.HelpGlyph}" ToolTip.Tip="{loc:Translate Options.Radio.Hamlib.LibraryPath.Help}" />
                                            <TextBox Grid.Column="2" Classes="Industry IndustryInput" Text="{Binding HamlibLibraryPath}"
                                                      Watermark="{loc:Translate Options.Radio.Hamlib.LibraryPath.Watermark}" IsEnabled="{Binding !IsProbingHamlib}" />
                                        </Grid>
                                        <TextBlock Classes="IndustryRowLabel" Text="{loc:Translate Options.Radio.Hamlib.LibraryPath.Caption}" TextWrapping="Wrap" />
                                        <WrapPanel>
                                            <Button Classes="Industry IndustryBtn IndustryBtn24 IndustryBtnSecondary" Margin="0,0,4,0"
                                                    Content="{loc:Translate Options.Radio.Hamlib.Browse}"
                                                    Command="{Binding BrowseHamlibLibraryCommand}" />
                                            <Button Classes="Industry IndustryBtn IndustryBtn24 IndustryBtnSecondary" Margin="0,0,4,0"
                                                    Content="{loc:Translate Options.Radio.Hamlib.AutoDetect}"
                                                    Command="{Binding AutoDetectHamlibCommand}" />
                                            <Button Classes="Industry IndustryBtn IndustryBtn24 IndustryBtnSecondary"
                                                    Content="{loc:Translate Options.Radio.Hamlib.TestPath}"
                                                    Command="{Binding ProbeHamlibCommand}" />
                                        </WrapPanel>
                                        <TextBlock Classes="IndustryRowValue" TextWrapping="Wrap"
                                                   Text="{Binding HamlibDiscoveryStatusMessage}"
                                                   IsVisible="{Binding HamlibDiscoveryStatusMessage, Converter={x:Static ObjectConverters.IsNotNull}}" />

                                        <Grid ColumnDefinitions="Auto,*">
                                            <TextBlock Classes="IndustryRowLabel" VerticalAlignment="Center" Text="{loc:Translate Options.Radio.Hamlib.RigList}" />
                                            <!-- Real-window check found a genuine Avalonia ComboBox
                                                 quirk here (confirmed not a screenshot artifact — it
                                                 persisted after the mouse moved away and the frame
                                                 settled): with an empty ItemsSource and no selection,
                                                 the control speculatively renders ItemTemplate once
                                                 against the wrong (ambient) DataContext, so
                                                 Manufacturer/ModelName resolve to Avalonia's unset-value
                                                 marker and render as the literal text "(unset)".
                                                 FallbackValue="" on both bindings makes that speculative
                                                 render produce an empty string instead, so it neither
                                                 shows garbage nor visually collides with
                                                 PlaceholderText. -->
                                            <ComboBox Grid.Column="1" Classes="Industry IndustryInput" ItemsSource="{Binding HamlibRigModels}"
                                                      SelectedItem="{Binding SelectedHamlibRigModel}"
                                                      PlaceholderText="{loc:Translate Options.Radio.Hamlib.RigList.Placeholder}"
                                                      HorizontalAlignment="Stretch">
                                                <ComboBox.ItemTemplate>
                                                    <DataTemplate>
                                                        <TextBlock>
                                                            <TextBlock.Text>
                                                                <MultiBinding StringFormat="{}{0} {1}">
                                                                    <Binding Path="Manufacturer" FallbackValue="" />
                                                                    <Binding Path="ModelName" FallbackValue="" />
                                                                </MultiBinding>
                                                            </TextBlock.Text>
                                                        </TextBlock>
                                                    </DataTemplate>
                                                </ComboBox.ItemTemplate>
                                            </ComboBox>
                                        </Grid>

                                        <Grid ColumnDefinitions="Auto,Auto,*">
                                            <TextBlock Classes="IndustryRowLabel" VerticalAlignment="Center" Text="{loc:Translate Options.Radio.Hamlib.Model}" />
                                            <TextBlock Grid.Column="1" Classes="IndustryHelpGlyph" Margin="{StaticResource IndustryPaddingHairline}" Text="{loc:Translate Options.HelpGlyph}" ToolTip.Tip="{loc:Translate Options.Radio.Hamlib.Model.Help}" />
                                            <TextBox Grid.Column="2" Classes="Industry IndustryInput" Text="{Binding HamlibModel}" />
                                        </Grid>

                                        <Grid ColumnDefinitions="Auto,Auto,*">
                                            <TextBlock Classes="IndustryRowLabel" VerticalAlignment="Center" Text="{loc:Translate Options.Radio.Hamlib.SerialPort}" />
                                            <TextBlock Grid.Column="1" Classes="IndustryHelpGlyph" Margin="{StaticResource IndustryPaddingHairline}" Text="{loc:Translate Options.HelpGlyph}" ToolTip.Tip="{loc:Translate Options.Radio.Hamlib.SerialPort.Help}" />
                                            <Grid Grid.Column="2" ColumnDefinitions="*,Auto">
                                                <ComboBox Classes="Industry IndustryInput" ItemsSource="{Binding AvailableSerialPorts}"
                                                          Text="{Binding HamlibSerialPort}" IsEditable="True"
                                                          HorizontalAlignment="Stretch" />
                                                <Button Grid.Column="1" Classes="Industry IndustryBtn IndustryBtn24 IndustryBtnSecondary" Margin="4,0,0,0"
                                                        Content="{loc:Translate Options.Radio.Hamlib.SerialPort.Refresh}"
                                                        Command="{Binding RefreshSerialPortsCommand}" />
                                            </Grid>
                                        </Grid>

                                        <Grid ColumnDefinitions="Auto,*">
                                            <TextBlock Classes="IndustryRowLabel" VerticalAlignment="Center" Text="{loc:Translate Options.Radio.Hamlib.BaudRate}" />
                                            <!-- Code-review finding: SelectedItem alone silently erases
                                                 a persisted value outside this fixed list on the next
                                                 Save (e.g. an unusual rate the old free-text field
                                                 allowed) — editable, same as the serial port fields
                                                 above, so an out-of-list value round-trips instead. -->
                                            <ComboBox Grid.Column="1" Classes="Industry IndustryInput" ItemsSource="{Binding AvailableBaudRates}"
                                                      Text="{Binding HamlibBaudRate}" IsEditable="True" HorizontalAlignment="Left" />
                                        </Grid>

                                        <StackPanel Orientation="Horizontal" Spacing="{StaticResource IndustrySpacingTight}">
                                            <TextBlock Classes="IndustryRowLabel" VerticalAlignment="Center" Text="{loc:Translate Options.Radio.Hamlib.PttType}" />
                                            <TextBlock Classes="IndustryHelpGlyph" Text="{loc:Translate Options.HelpGlyph}" ToolTip.Tip="{loc:Translate Options.Radio.Hamlib.PttType.Help}" />
                                        </StackPanel>
                                        <WrapPanel>
                                            <RadioButton Classes="Industry IndustryMini" Theme="{StaticResource IndustryMiniRadioTheme}" Margin="0,0,4,4"
                                                         GroupName="PttMethod"
                                                         Content="{loc:Translate Options.Radio.Hamlib.PttMethod.Vox}"
                                                         IsChecked="{Binding IsPttMethodVoxSelected}" />
                                            <RadioButton Classes="Industry IndustryMini" Theme="{StaticResource IndustryMiniRadioTheme}" Margin="0,0,4,4"
                                                         GroupName="PttMethod"
                                                         Content="{loc:Translate Options.Radio.Hamlib.PttMethod.Cat}"
                                                         IsChecked="{Binding IsPttMethodCatSelected}" />
                                            <RadioButton Classes="Industry IndustryMini" Theme="{StaticResource IndustryMiniRadioTheme}" Margin="0,0,4,4"
                                                         GroupName="PttMethod"
                                                         Content="{loc:Translate Options.Radio.Hamlib.PttMethod.Rts}"
                                                         IsChecked="{Binding IsPttMethodRtsSelected}" />
                                            <RadioButton Classes="Industry IndustryMini" Theme="{StaticResource IndustryMiniRadioTheme}" Margin="0,0,4,4"
                                                         GroupName="PttMethod"
                                                         Content="{loc:Translate Options.Radio.Hamlib.PttMethod.Dtr}"
                                                         IsChecked="{Binding IsPttMethodDtrSelected}" />
                                        </WrapPanel>

                                        <Grid ColumnDefinitions="Auto,Auto,*" IsEnabled="{Binding IsPttPortEnabled}">
                                            <TextBlock Classes="IndustryRowLabel" VerticalAlignment="Center" Text="{loc:Translate Options.Radio.Hamlib.PttPort}" />
                                            <TextBlock Grid.Column="1" Classes="IndustryHelpGlyph" Margin="{StaticResource IndustryPaddingHairline}" Text="{loc:Translate Options.HelpGlyph}" ToolTip.Tip="{loc:Translate Options.Radio.Hamlib.PttPort.Help}" />
                                            <ComboBox Grid.Column="2" Classes="Industry IndustryInput" ItemsSource="{Binding AvailableSerialPorts}"
                                                      Text="{Binding HamlibPttPort}" IsEditable="True"
                                                      HorizontalAlignment="Stretch" />
                                        </Grid>

                                        <WrapPanel>
                                            <Button Classes="Industry IndustryBtn IndustryBtn24 IndustryBtnSecondary" Margin="0,0,4,0"
                                                    Content="{loc:Translate Options.Radio.Hamlib.TestCat}"
                                                    Command="{Binding TestHamlibConnectionCommand}" />
                                            <Button Classes="Industry IndustryBtn IndustryBtn24 IndustryBtnSecondary"
                                                    Content="{Binding TestPttButtonLabel}"
                                                    Command="{Binding TestPttCommand}"
                                                    IsEnabled="{Binding CanTestPtt}"
                                                    ToolTip.Tip="{loc:Translate Options.Radio.Hamlib.TestPtt.VoxDisabled.Help}" />
                                        </WrapPanel>
                                        <TextBlock Classes="IndustryRowValue" VerticalAlignment="Center"
                                                   TextWrapping="Wrap"
                                                   Text="{Binding TestConnectionStatusMessage}"
                                                   IsVisible="{Binding TestConnectionStatusMessage, Converter={x:Static ObjectConverters.IsNotNull}}" />
                                        <TextBlock Classes="IndustryRowValue" VerticalAlignment="Center"
                                                   TextWrapping="Wrap"
                                                   Text="{Binding TestPttErrorMessage}"
                                                   IsVisible="{Binding TestPttErrorMessage, Converter={x:Static ObjectConverters.IsNotNull}}" />
                                    </StackPanel>

                                    <StackPanel IsVisible="{Binding IsFlrigSelected}" Spacing="{StaticResource IndustrySpacingSnug}">
                                        <Grid ColumnDefinitions="Auto,*">
                                            <TextBlock Classes="IndustryRowLabel" VerticalAlignment="Center" Text="{loc:Translate Options.Radio.Host}" />
                                            <TextBox Grid.Column="1" Classes="Industry IndustryInput" Text="{Binding FlrigHost}" />
                                        </Grid>
                                        <Grid ColumnDefinitions="Auto,*">
                                            <TextBlock Classes="IndustryRowLabel" VerticalAlignment="Center" Text="{loc:Translate Options.Radio.Port}" />
                                            <NumericUpDown Grid.Column="1" Classes="Industry" Theme="{StaticResource IndustryStepperTheme}" Value="{Binding FlrigPort}" HorizontalAlignment="Left" />
                                        </Grid>
                                        <!-- Two tests, mirroring the Hamlib panel's CAT/PTT split
                                             above (not rigctld's plain single "Test connection") —
                                             flrig's PTT is a real hardware-keying action through the
                                             connected rig, needing the same explicit-test-before-
                                             Connect safety gate Hamlib's CAT PTT method already has,
                                             with no VOX-style exemption (flrig has exactly one PTT
                                             mechanism). -->
                                        <WrapPanel>
                                            <Button Classes="Industry IndustryBtn IndustryBtn24 IndustryBtnSecondary" Margin="0,0,4,0"
                                                    Content="{loc:Translate Options.Radio.Flrig.TestConnection}"
                                                    Command="{Binding TestFlrigConnectionCommand}" />
                                            <Button Classes="Industry IndustryBtn IndustryBtn24 IndustryBtnSecondary"
                                                    Content="{Binding TestFlrigPttButtonLabel}"
                                                    Command="{Binding TestFlrigPttCommand}" />
                                        </WrapPanel>
                                        <TextBlock Classes="IndustryRowValue" VerticalAlignment="Center"
                                                   TextWrapping="Wrap"
                                                   Text="{Binding TestConnectionStatusMessage}"
                                                   IsVisible="{Binding TestConnectionStatusMessage, Converter={x:Static ObjectConverters.IsNotNull}}" />
                                        <TextBlock Classes="IndustryRowValue" VerticalAlignment="Center"
                                                   TextWrapping="Wrap"
                                                   Text="{Binding TestPttErrorMessage}"
                                                   IsVisible="{Binding TestPttErrorMessage, Converter={x:Static ObjectConverters.IsNotNull}}" />
                                    </StackPanel>

                                    <!-- No host/port field, unlike the other backends —
                                         OmniRigConnectionSpec is parameterless; OmniRig's own config
                                         dialog owns rig selection (spec/03-cat-layer.md's OmniRig
                                         section). No separate PTT test either — see
                                         OmniRigTestSucceeded's own doc comment. -->
                                    <StackPanel IsVisible="{Binding IsOmniRigSelected}" Spacing="{StaticResource IndustrySpacingSnug}">
                                        <Button Classes="Industry IndustryBtn IndustryBtn24 IndustryBtnSecondary" HorizontalAlignment="Left"
                                                Content="{loc:Translate Options.Radio.OmniRig.TestConnection}"
                                                Command="{Binding TestOmniRigConnectionCommand}" />
                                        <TextBlock Classes="IndustryRowValue" VerticalAlignment="Center"
                                                   TextWrapping="Wrap"
                                                   Text="{Binding TestConnectionStatusMessage}"
                                                   IsVisible="{Binding TestConnectionStatusMessage, Converter={x:Static ObjectConverters.IsNotNull}}" />
                                    </StackPanel>
                                </StackPanel>
                            </ContentControl>

                            <!-- RTS on RX (legacy CBRTS) and PTT Lock (legacy PTTLock) both removed
                                 2026-08-28 — see docs/removed-features.md's "Raw-serial RTS-pin PTT
                                 keying" entry. Neither has a hookable equivalent in this port's
                                 architecture (PTT goes through IRadioController.SetPttAsync via
                                 whichever CAT backend is configured, which owns its own connection
                                 lifecycle) with one narrow exception found and deliberately NOT
                                 exposed: Hamlib's own ptt_share config key is a near-equivalent to
                                 PTT Lock specifically, reachable via the same ApplyConf path
                                 HamlibRadioProtocol.cs already uses for other settings — the port
                                 chose removal anyway (direct user decision) rather than building a
                                 narrow, Hamlib-only, DTR/RTS-only exposure of one config key. -->

                            <!-- Pwr + Tune: direct-request feature (user quote: "TUNE button that
                                 works like WSJTX ... so that we can set our Pwr slider to the TX
                                 power output we want"). Deliberately NOT gated behind this dialog's
                                 usual Save button — see TxVolumePercent's own doc comment for why;
                                 this is the same live, debounced-persist shape as the header's own
                                 Pwr slider, not part of the edit-buffer-committed-on-Save pattern
                                 every other field on this tab follows. -->
                            <HeaderedContentControl Classes="Industry" Theme="{StaticResource IndustryGroupBoxTheme}">
                                <HeaderedContentControl.Header>
                                    <TextBlock Classes="IndustryGbTitle" Text="{loc:Translate Options.Radio.PwrHeader}" />
                                </HeaderedContentControl.Header>
                                <StackPanel Spacing="{StaticResource IndustrySpacingSnug}">
                                    <Grid ColumnDefinitions="Auto,*,Auto" ColumnSpacing="6">
                                        <TextBlock Classes="IndustryRowLabel" Text="{loc:Translate Options.Radio.PwrLabel}" VerticalAlignment="Center" />
                                        <Slider Grid.Column="1" Classes="Industry h6" Theme="{StaticResource IndustrySliderTheme}"
                                                Value="{Binding TxVolumePercent}" Minimum="0" Maximum="100" VerticalAlignment="Center" />
                                        <TextBlock Grid.Column="2" Classes="IndustryRowValue" Text="{Binding TxVolumeDisplay}" VerticalAlignment="Center" />
                                    </Grid>
                                    <StackPanel Orientation="Horizontal" Spacing="{StaticResource IndustrySpacingTight}">
                                        <Button Classes="Industry IndustryBtn IndustryBtn24 IndustryBtnSecondary"
                                                Content="{Binding TuneButtonLabel}"
                                                Command="{Binding TuneCommand}"
                                                ToolTip.Tip="{loc:Translate Options.Radio.Tune.Help}" />
                                        <TextBlock Classes="IndustryRowValue" VerticalAlignment="Center"
                                                   Text="{Binding TuneErrorMessage}"
                                                   Foreground="{DynamicResource IndustryDanger}"
                                                   IsVisible="{Binding TuneErrorMessage, Converter={x:Static ObjectConverters.IsNotNull}}" />
                                    </StackPanel>
                                </StackPanel>
                            </HeaderedContentControl>

                            <!-- SWR auto-cutoff (2026-08-26, relocated here from the TX Controls
                                 pane's own Output card per user request — see
                                 OptionsWindowViewModel.SwrCutoffEnabled's own doc comment). Real,
                                 enforced control — see TxControlsPaneViewModel.CheckSwrCutoff, which
                                 picks up a Save made here live via IRadioSessionService.
                                 SafetySettingsChanged, without needing a restart. Deliberately
                                 UNCONDITIONALLY editable (no IsEnabled gate on live capability, unlike
                                 the control this replaced) — Options configures ahead of a
                                 connection; ShowSwrCutoffCapabilityHint below is the non-disabling
                                 substitute for that old greying. -->
                            <HeaderedContentControl Classes="Industry" Theme="{StaticResource IndustryGroupBoxTheme}">
                                <HeaderedContentControl.Header>
                                    <TextBlock Classes="IndustryGbTitle" Text="{loc:Translate Options.Radio.SafetyHeader}" />
                                </HeaderedContentControl.Header>
                                <StackPanel Spacing="{StaticResource IndustrySpacingSnug}">
                                    <CheckBox Classes="Industry" Theme="{StaticResource IndustryCheckBoxTheme}"
                                              Content="{loc:Translate Options.Radio.SwrCutoffEnabled}"
                                              IsChecked="{Binding SwrCutoffEnabled}"
                                              ToolTip.Tip="{loc:Translate Options.Radio.SwrCutoffEnabled.Help}" />
                                    <Grid ColumnDefinitions="Auto,Auto" ColumnSpacing="6">
                                        <TextBlock Classes="IndustryRowLabel" VerticalAlignment="Center" Text="{loc:Translate Options.Radio.SwrCutoffThreshold}" />
                                        <NumericUpDown Grid.Column="1" Classes="Industry" Theme="{StaticResource IndustryStepperTheme}"
                                                       Value="{Binding SwrCutoffThreshold}" Increment="0.1" Minimum="1.0" Maximum="10.0" FormatString="0.0" />
                                    </Grid>
                                    <TextBlock Classes="IndustryRowValue" TextWrapping="Wrap"
                                               Text="{loc:Translate Options.Radio.SwrCutoffCapabilityHint}"
                                               Foreground="{DynamicResource IndustryDanger}"
                                               IsVisible="{Binding ShowSwrCutoffCapabilityHint}" />
                                </StackPanel>
                            </HeaderedContentControl>

                            <Button Classes="Industry IndustryBtn IndustryBtn24 IndustryBtnSecondary" Padding="7,0" Content="{loc:Translate Options.ResetSection}" Command="{Binding ResetRadioToDefaultCommand}" />
                        </StackPanel>
                    </ScrollViewer>
                </TabItem>

                <TabItem Classes="Industry" Header="{loc:Translate Options.Tx.Tab}">
                    <ScrollViewer>
                        <StackPanel Spacing="{StaticResource IndustrySpacingGroup}" Margin="{StaticResource IndustryPaddingGroup}">
                            <ContentControl Classes="Industry" Theme="{StaticResource IndustryBlueprintBorderTheme}" Padding="{StaticResource IndustryGbPadding}">
                                <StackPanel Spacing="{StaticResource IndustrySpacingSnug}">
                                    <StackPanel Orientation="Horizontal" Spacing="{StaticResource IndustrySpacingTight}">
                                        <TextBlock Classes="IndustryRowLabel" VerticalAlignment="Center" Text="{loc:Translate Options.Tx.Callsign}" />
                                        <TextBlock Classes="IndustryHelpGlyph" Text="{loc:Translate Options.HelpGlyph}" ToolTip.Tip="{loc:Translate Options.Tx.Callsign.Help}" />
                                    </StackPanel>
                                    <TextBox Classes="Industry IndustryInput" Text="{Binding Callsign}" HorizontalAlignment="Stretch" />
                                </StackPanel>
                            </ContentControl>

                            <!-- Name/Grid: no legacy MMSSTV equivalent (its own macro engine has no
                                 "my name"/"my QTH" token at all), added to back mock2's own "MY
                                 NAME"/"MY GRID" overlay insert-field chips and the TX-macro engine.
                                 Default RST (2026-09-01), the third row below, DOES have a legacy
                                 equivalent — Main.cpp's LogRST combo default and LogSet.cpp's
                                 DefMyRST — see OperatorSettings.DefaultRst's own doc comment. -->
                            <ContentControl Classes="Industry" Theme="{StaticResource IndustryBlueprintBorderTheme}" Padding="{StaticResource IndustryGbPadding}">
                                <StackPanel Spacing="{StaticResource IndustrySpacingSnug}">
                                    <Grid ColumnDefinitions="Auto,*">
                                        <TextBlock Classes="IndustryRowLabel" VerticalAlignment="Center" Text="{loc:Translate Options.Tx.OperatorName}" />
                                        <TextBox Grid.Column="1" Classes="Industry IndustryInput" Text="{Binding OperatorName}" />
                                    </Grid>
                                    <Grid ColumnDefinitions="Auto,*">
                                        <TextBlock Classes="IndustryRowLabel" VerticalAlignment="Center" Text="{loc:Translate Options.Tx.OperatorGrid}" />
                                        <TextBox Grid.Column="1" Classes="Industry IndustryInput" Text="{Binding OperatorGrid}" />
                                    </Grid>
                                    <Grid ColumnDefinitions="Auto,*">
                                        <StackPanel Orientation="Horizontal" Spacing="{StaticResource IndustrySpacingTight}">
                                            <TextBlock Classes="IndustryRowLabel" VerticalAlignment="Center" Text="{loc:Translate Options.Tx.DefaultRst}" />
                                            <TextBlock Classes="IndustryHelpGlyph" Text="{loc:Translate Options.HelpGlyph}" ToolTip.Tip="{loc:Translate Options.Tx.DefaultRst.Help}" />
                                        </StackPanel>
                                        <TextBox Grid.Column="1" Classes="Industry IndustryInput" Text="{Binding DefaultRst}" />
                                    </Grid>
                                </StackPanel>
                            </ContentControl>

                            <!-- Removed 2026-08-12: this disabled QRZ.com-lookup placeholder is now
                                 superseded by the real "QRZ.com" tab (QrzLookupEnabled/Username/
                                 Password, wired in commit 4f14396). Leaving both would mean two
                                 different "QRZ lookup" toggles in two different tabs, one real and
                                 one a dead stub, which reads as a bug, not a feature. -->

                            <Button Classes="Industry IndustryBtn IndustryBtn24 IndustryBtnSecondary" Padding="7,0" Content="{loc:Translate Options.ResetSection}" Command="{Binding ResetTxToDefaultCommand}" />
                        </StackPanel>
                    </ScrollViewer>
                </TabItem>

                <!-- RX/demod behavior tab. Auto-Sync/Auto-Slant wired 2026-08-11, Auto-stop/
                     Auto-restart + Sense level wired 2026-08-12, Demod type wired 2026-08-12
                     (demod-type runtime-dispatch subsystem, all 4 phases), RX BPF wired 2026-08-12
                     (RX BPF subsystem, all 4 phases: CalcBPF-equivalent FIR filter-preset port,
                     including the new Kaiser-Bessel-window math Narrow/VeryNarrow need), RX buffer
                     wired 2026-08-15 (RX buffer subsystem Phase 9, the whole 9-phase subsystem's
                     final piece: a real buffered-line-replay/Correct-Slant search backs it now,
                     not a stub), all real SstvDecoderSettings fields, restart-required. Auto-start
                     removed 2026-08-25 (elegant-wondering-hinton.md): its real legacy behavior is
                     the RX-page SBAuto toggle, ported instead as the Receive tab's own real
                     "Listening / Paused" segment (MainWindow.axaml) — not a settings-dialog
                     checkbox. -->
                <TabItem Classes="Industry" Header="{loc:Translate Options.Decode.Tab}">
                    <ScrollViewer>
                        <StackPanel Spacing="{StaticResource IndustrySpacingGroup}" Margin="{StaticResource IndustryPaddingGroup}">
                            <ContentControl Classes="Industry" Theme="{StaticResource IndustryBlueprintBorderTheme}" Padding="{StaticResource IndustryGbPadding}">
                                <StackPanel Spacing="{StaticResource IndustrySpacingSnug}">
                                    <StackPanel Orientation="Horizontal" Spacing="{StaticResource IndustrySpacingTight}">
                                        <TextBlock Classes="IndustryRowLabel" VerticalAlignment="Center" Text="{loc:Translate Options.Decode.SenseLevel}" />
                                        <TextBlock Classes="IndustryHelpGlyph" Text="{loc:Translate Options.HelpGlyph}" ToolTip.Tip="{loc:Translate Options.Decode.SenseLevel.Help}" />
                                    </StackPanel>
                                    <WrapPanel>
                                        <RadioButton Classes="Industry IndustryMini" Theme="{StaticResource IndustryMiniRadioTheme}" Margin="0,0,4,4" GroupName="SenseLevel" Content="{loc:Translate Options.Decode.SenseLevel.VeryLow}" IsChecked="{Binding IsSenseLevelVeryLowSelected}" />
                                        <RadioButton Classes="Industry IndustryMini" Theme="{StaticResource IndustryMiniRadioTheme}" Margin="0,0,4,4" GroupName="SenseLevel" Content="{loc:Translate Options.Decode.SenseLevel.Low}" IsChecked="{Binding IsSenseLevelLowSelected}" />
                                        <RadioButton Classes="Industry IndustryMini" Theme="{StaticResource IndustryMiniRadioTheme}" Margin="0,0,4,4" GroupName="SenseLevel" Content="{loc:Translate Options.Decode.SenseLevel.High}" IsChecked="{Binding IsSenseLevelHighSelected}" />
                                        <RadioButton Classes="Industry IndustryMini" Theme="{StaticResource IndustryMiniRadioTheme}" Margin="0,0,4,4" GroupName="SenseLevel" Content="{loc:Translate Options.Decode.SenseLevel.VeryHigh}" IsChecked="{Binding IsSenseLevelVeryHighSelected}" />
                                    </WrapPanel>

                                    <StackPanel Orientation="Horizontal" Spacing="{StaticResource IndustrySpacingTight}">
                                        <TextBlock Classes="IndustryRowLabel" VerticalAlignment="Center" Text="{loc:Translate Options.Decode.RxBpf}" />
                                        <TextBlock Classes="IndustryHelpGlyph" Text="{loc:Translate Options.HelpGlyph}" ToolTip.Tip="{loc:Translate Options.Decode.RxBpf.Help}" />
                                    </StackPanel>
                                    <WrapPanel>
                                        <RadioButton Classes="Industry IndustryMini" Theme="{StaticResource IndustryMiniRadioTheme}" Margin="0,0,4,4" GroupName="RxBpf" Content="{loc:Translate Options.Decode.RxBpf.Normal}" IsChecked="{Binding IsRxBpfOffSelected}" />
                                        <RadioButton Classes="Industry IndustryMini" Theme="{StaticResource IndustryMiniRadioTheme}" Margin="0,0,4,4" GroupName="RxBpf" Content="{loc:Translate Options.Decode.RxBpf.Wide}" IsChecked="{Binding IsRxBpfWideSelected}" />
                                        <RadioButton Classes="Industry IndustryMini" Theme="{StaticResource IndustryMiniRadioTheme}" Margin="0,0,4,4" GroupName="RxBpf" Content="{loc:Translate Options.Decode.RxBpf.Sharp}" IsChecked="{Binding IsRxBpfNarrowSelected}" />
                                        <RadioButton Classes="Industry IndustryMini" Theme="{StaticResource IndustryMiniRadioTheme}" Margin="0,0,4,4" GroupName="RxBpf" Content="{loc:Translate Options.Decode.RxBpf.VerySharp}" IsChecked="{Binding IsRxBpfVeryNarrowSelected}" />
                                    </WrapPanel>

                                    <StackPanel Orientation="Horizontal" Spacing="{StaticResource IndustrySpacingTight}">
                                        <TextBlock Classes="IndustryRowLabel" VerticalAlignment="Center" Text="{loc:Translate Options.Decode.DemodType}" />
                                        <TextBlock Classes="IndustryHelpGlyph" Text="{loc:Translate Options.HelpGlyph}" ToolTip.Tip="{loc:Translate Options.Decode.DemodType.Help}" />
                                    </StackPanel>
                                    <WrapPanel>
                                        <RadioButton Classes="Industry IndustryMini" Theme="{StaticResource IndustryMiniRadioTheme}" Margin="0,0,4,4" GroupName="DemodType" Content="{loc:Translate Options.Decode.DemodType.Pll}" IsChecked="{Binding IsDemodTypePllSelected}" />
                                        <RadioButton Classes="Industry IndustryMini" Theme="{StaticResource IndustryMiniRadioTheme}" Margin="0,0,4,4" GroupName="DemodType" Content="{loc:Translate Options.Decode.DemodType.ZeroCrossing}" IsChecked="{Binding IsDemodTypeZeroCrossingSelected}" />
                                        <RadioButton Classes="Industry IndustryMini" Theme="{StaticResource IndustryMiniRadioTheme}" Margin="0,0,4,4" GroupName="DemodType" Content="{loc:Translate Options.Decode.DemodType.Hilbert}" IsChecked="{Binding IsDemodTypeHilbertSelected}" />
                                    </WrapPanel>

                                    <StackPanel Orientation="Horizontal" Spacing="{StaticResource IndustrySpacingTight}">
                                        <TextBlock Classes="IndustryRowLabel" VerticalAlignment="Center" Text="{loc:Translate Options.Decode.RxBuffer}" />
                                        <TextBlock Classes="IndustryHelpGlyph" Text="{loc:Translate Options.HelpGlyph}" ToolTip.Tip="{loc:Translate Options.Decode.RxBuffer.Help}" />
                                    </StackPanel>
                                    <WrapPanel>
                                        <RadioButton Classes="Industry IndustryMini" Theme="{StaticResource IndustryMiniRadioTheme}" Margin="0,0,4,4" GroupName="RxBuffer" Content="{loc:Translate Options.Decode.RxBuffer.Off}" IsChecked="{Binding IsRxBufferOffSelected}" />
                                        <RadioButton Classes="Industry IndustryMini" Theme="{StaticResource IndustryMiniRadioTheme}" Margin="0,0,4,4" GroupName="RxBuffer" Content="{loc:Translate Options.Decode.RxBuffer.On}" IsChecked="{Binding IsRxBufferOnSelected}" />
                                        <RadioButton Classes="Industry IndustryMini" Theme="{StaticResource IndustryMiniRadioTheme}" Margin="0,0,4,4" GroupName="RxBuffer" Content="{loc:Translate Options.Decode.RxBuffer.Extended}" IsChecked="{Binding IsRxBufferExtendedSelected}" />
                                    </WrapPanel>

                                    <!-- Wired 2026-08-12: SstvDecoderSettings.AutoStopEnabled/SyncRestartEnabled are
                                         real, already-consumed decoder settings; these two were left STUB
                                         specifically because their OLD loc text lied about what the fields actually
                                         gate (AutoStop said "when sync looks stable", the field really gates
                                         stopping on erratic/WEAK signal; AutoRestart said "on loss", the field
                                         really gates restarting onto a STRONGER sync found mid-reception): fixed
                                         both labels to match SstvDecoderSettings.cs's own verified doc comments
                                         before wiring, not shipping a control that still lies. Genuinely LIVE now
                                         (2026-08-27, restart-required-settings backlog item 1) — see
                                         OptionsWindowViewModel.AutoSyncEnabled's own doc comment. -->
                                    <CheckBox Classes="Industry" Theme="{StaticResource IndustryCheckBoxTheme}" Content="{loc:Translate Options.Decode.AutoStop}"
                                              IsChecked="{Binding AutoStopEnabled}" />
                                    <CheckBox Classes="Industry" Theme="{StaticResource IndustryCheckBoxTheme}" Content="{loc:Translate Options.Decode.AutoRestart}"
                                              IsChecked="{Binding SyncRestartEnabled}" />
                                    <!-- Wired 2026-08-11: SstvDecoderSettings.AutoSyncEnabled/AutoSlantEnabled are real,
                                         already-consumed decoder settings (RestartableSstvDecoder via
                                         ScanlineStudio.Host.Program's ISstvDecoder registration); this dialog was the
                                         only missing piece. Genuinely LIVE now (2026-08-27, restart-required-settings
                                         backlog item 1) — see OptionsWindowViewModel.AutoSyncEnabled's own doc
                                         comment. -->
                                    <CheckBox Classes="Industry" Theme="{StaticResource IndustryCheckBoxTheme}" Content="{loc:Translate Options.Decode.AutoSync}"
                                              IsChecked="{Binding AutoSyncEnabled}" />
                                    <!-- IsEnabled gating added RX buffer subsystem Phase 9: legacy's own
                                         CBASlant->Enabled = RGRBuf->ItemIndex ? TRUE : FALSE (Option.cpp:222).
                                         Auto Slant has no effect with RX buffering off. -->
                                    <CheckBox Classes="Industry" Theme="{StaticResource IndustryCheckBoxTheme}" Content="{loc:Translate Options.Decode.AutoSlant}"
                                              IsChecked="{Binding AutoSlantEnabled}" IsEnabled="{Binding IsAutoSlantRowEnabled}" />

                                    <Button Classes="Industry IndustryBtn IndustryBtn24 IndustryBtnSecondary" Padding="7,0" Content="{loc:Translate Options.ResetSection}" Command="{Binding ResetDecodeToDefaultCommand}" />
                                </StackPanel>
                            </ContentControl>
                        </StackPanel>
                    </ScrollViewer>
                </TabItem>

                <!-- ID method (CW/FSK) is real (CW-ID/FSK station-ID subsystem, Phase 6). Sound-file
                     ID, VOX, and the satellite-tune trigger stay disabled: none of those exist
                     yet. Tune frequency/duration themselves are real (header's Tune row) and
                     intentionally NOT duplicated here. NR/RST TX enable/text (wired 2026-08-15,
                     Tier 2) is a static, user-typed value, not a live "current QSO" exchange field:
                     StationIdSettings.NrRstText's own doc comment already made that call, not a
                     new decision here. The RX-side decoded-NR/RST display
                     (RxImagePaneViewModel.DecodedNrRst) is now bound to a real row on the RxFrameMeta
                     card (2026-08-26), not this Options dialog — unrelated to the TX-side field
                     documented above. -->
                <TabItem Classes="Industry" Header="{loc:Translate Options.Identification.Tab}">
                    <ScrollViewer>
                        <StackPanel Spacing="{StaticResource IndustrySpacingGroup}" Margin="{StaticResource IndustryPaddingGroup}">
                            <HeaderedContentControl Classes="Industry" Theme="{StaticResource IndustryGroupBoxTheme}">
                                <HeaderedContentControl.Header>
                                    <TextBlock Classes="IndustryGbTitle" Text="{loc:Translate Options.Identification.IdMethodHeader}" />
                                </HeaderedContentControl.Header>
                                <StackPanel Spacing="{StaticResource IndustrySpacingSnug}">
                                    <WrapPanel>
                                        <RadioButton Classes="Industry IndustryMini" Theme="{StaticResource IndustryMiniRadioTheme}" Margin="0,0,4,4" GroupName="IdMethod" Content="{loc:Translate Options.Identification.IdMethod.Off}" IsChecked="{Binding IsIdMethodOffSelected}" />
                                        <RadioButton Classes="Industry IndustryMini" Theme="{StaticResource IndustryMiniRadioTheme}" Margin="0,0,4,4" GroupName="IdMethod" Content="{loc:Translate Options.Identification.IdMethod.Cw}" IsChecked="{Binding IsIdMethodCwSelected}" />
                                        <RadioButton Classes="Industry IndustryMini" Theme="{StaticResource IndustryMiniRadioTheme}" Margin="0,0,4,4" GroupName="IdMethod" Content="{loc:Translate Options.Identification.IdMethod.SoundFile}" IsChecked="{Binding IsIdMethodSoundFileSelected}" />
                                    </WrapPanel>

                                    <StackPanel IsVisible="{Binding IsIdMethodCwSelected}" Spacing="{StaticResource IndustrySpacingSnug}">
                                        <TextBlock Classes="IndustryRowLabel" Text="{loc:Translate Options.Identification.CwText}" />
                                        <TextBox Classes="Industry IndustryInput" Text="{Binding CwText}" />

                                        <Grid ColumnDefinitions="Auto,*">
                                            <TextBlock Classes="IndustryRowLabel" VerticalAlignment="Center" Text="{loc:Translate Options.Identification.CwFrequency}" />
                                            <NumericUpDown Grid.Column="1" Classes="Industry" Theme="{StaticResource IndustryStepperTheme}" Value="{Binding CwToneFrequencyHz}" Minimum="100" Maximum="3000" HorizontalAlignment="Left" />
                                        </Grid>
                                        <Grid ColumnDefinitions="Auto,*">
                                            <TextBlock Classes="IndustryRowLabel" VerticalAlignment="Center" Text="{loc:Translate Options.Identification.CwSpeed}" />
                                            <NumericUpDown Grid.Column="1" Classes="Industry" Theme="{StaticResource IndustryStepperTheme}" Value="{Binding CwWpm}" Minimum="10" Maximum="50" HorizontalAlignment="Left" />
                                        </Grid>
                                    </StackPanel>

                                    <StackPanel IsVisible="{Binding IsIdMethodSoundFileSelected}" Orientation="Horizontal" Spacing="{StaticResource IndustrySpacingTight}">
                                        <TextBlock Classes="IndustryRowLabel" VerticalAlignment="Center" Text="{loc:Translate Options.Identification.SoundFile}" />
                                        <TextBlock Classes="IndustryHelpGlyph" Text="{loc:Translate Options.HelpGlyph}" ToolTip.Tip="{loc:Translate Options.Identification.SoundFile.Help}" />
                                    </StackPanel>
                                    <Grid IsVisible="{Binding IsIdMethodSoundFileSelected}" ColumnDefinitions="*,Auto" ColumnSpacing="{StaticResource IndustrySpacingSnug}">
                                        <TextBox Classes="Industry IndustryInput" Text="{Binding SoundFileMmvPath}" />
                                        <Button Grid.Column="1" Classes="Industry IndustryBtn IndustryBtn24 IndustryBtnSecondary" Padding="7,0" Content="{loc:Translate Options.Identification.SoundFile.Browse}" Command="{Binding BrowseSoundFileCommand}" />
                                    </Grid>
                                    <StackPanel IsVisible="{Binding IsIdMethodSoundFileSelected}" Spacing="{StaticResource IndustrySpacingSnug}">
                                        <TextBlock Classes="IndustryRowValue" TextWrapping="Wrap"
                                                   Text="{Binding SoundFileIdSuccessMessage}"
                                                   IsVisible="{Binding SoundFileIdSuccessMessage, Converter={x:Static ObjectConverters.IsNotNull}}" />
                                        <TextBlock Classes="IndustryRowValue" Foreground="{DynamicResource IndustryDanger}" TextWrapping="Wrap"
                                                   Text="{Binding SoundFileIdErrorMessage}"
                                                   IsVisible="{Binding SoundFileIdErrorMessage, Converter={x:Static ObjectConverters.IsNotNull}}" />
                                    </StackPanel>

                                    <CheckBox Classes="Industry" Theme="{StaticResource IndustryCheckBoxTheme}" Content="{loc:Translate Options.Identification.FskEncode}"
                                              IsChecked="{Binding FskIdTxEnabled}" />
                                    <CheckBox Classes="Industry" Theme="{StaticResource IndustryCheckBoxTheme}" Content="{loc:Translate Options.Identification.FskDecode}"
                                              IsChecked="{Binding FskIdRxEnabled}" />

                                    <CheckBox Classes="Industry" Theme="{StaticResource IndustryCheckBoxTheme}" Content="{loc:Translate Options.Identification.CwDecode}"
                                              IsChecked="{Binding CwIdRxEnabled}" />
                                    <Grid IsVisible="{Binding CwIdRxEnabled}" ColumnDefinitions="Auto,*">
                                        <TextBlock Classes="IndustryRowLabel" VerticalAlignment="Center" Text="{loc:Translate Options.Identification.CwDecodeWindowSeconds}" />
                                        <NumericUpDown Grid.Column="1" Classes="Industry" Theme="{StaticResource IndustryStepperTheme}" Value="{Binding CwIdRxWindowSeconds}" Minimum="5" Maximum="20" HorizontalAlignment="Left" />
                                    </Grid>

                                    <CheckBox Classes="Industry" Theme="{StaticResource IndustryCheckBoxTheme}" Content="{loc:Translate Options.Identification.NrRstEnabled}"
                                              IsChecked="{Binding NrRstEnabled}" />
                                    <StackPanel IsVisible="{Binding NrRstEnabled}" Spacing="{StaticResource IndustrySpacingSnug}">
                                        <TextBlock Classes="IndustryRowLabel" Text="{loc:Translate Options.Identification.NrRstText}" />
                                        <TextBox Classes="Industry IndustryInput" Watermark="{loc:Translate Options.Identification.NrRstTextWatermark}" Text="{Binding NrRstText}" />
                                    </StackPanel>
                                </StackPanel>
                            </HeaderedContentControl>

                            <!-- VOX leader-tone priming (legacy sys.m_VOX/m_VOXSound) REMOVED
                                 2026-08-28, direct user request — see docs/removed-features.md's own
                                 entry. -->

                            <!-- "Tune satellite" (legacy sys.m_TuneSat) removed 2026-08-28 — direct
                                 user decision, real legacy behavior confirmed first (auto-switch to
                                 TX when the Tune button's own timer elapses); see
                                 docs/removed-features.md's "Tune-timer auto-switch-to-transmit" entry. -->

                            <Button Classes="Industry IndustryBtn IndustryBtn24 IndustryBtnSecondary" Padding="7,0" Content="{loc:Translate Options.ResetSection}" Command="{Binding ResetIdentificationToDefaultCommand}" />
                        </StackPanel>
                    </ScrollViewer>
                </TabItem>

                <!-- Deep DSP/diagnostic internals — every row here is now real (Options stub
                     backlog items 1-3, see each group's own doc comment below), not a placeholder.
                     Filter-response preview buttons (legacy DispTxBpf/DispLoopLPF/DispOutLPF/
                     DispCrossOutLPF/RxBpfBtn) are still intentionally not included — they'd need a
                     real live filter-response plot, a separate, unbuilt piece of work, not blocked
                     on the underlying settings being wired (which they now are). -->
                <TabItem Classes="Industry" Header="{loc:Translate Options.Advanced.Tab}">
                    <ScrollViewer>
                        <StackPanel Spacing="{StaticResource IndustrySpacingGroup}" Margin="{StaticResource IndustryPaddingGroup}">
                            <TextBlock Foreground="{DynamicResource IndustryLabel}" FontFamily="{StaticResource IndustryBodyFontFamily}" FontSize="{DynamicResource IndustryOptionsAdvancedCaptionFontSize}" TextWrapping="Wrap" Text="{loc:Translate Options.Advanced.Caption}" />

                            <HeaderedContentControl Classes="Industry" Theme="{StaticResource IndustryGroupBoxTheme}">
                                <HeaderedContentControl.Header>
                                    <StackPanel Orientation="Horizontal" Spacing="{StaticResource IndustrySpacingTight}">
                                        <TextBlock Classes="IndustryGbTitle" Text="{loc:Translate Options.Advanced.PllHeader}" />
                                        <TextBlock Classes="IndustryHelpGlyph" Text="{loc:Translate Options.HelpGlyph}" ToolTip.Tip="{loc:Translate Options.Advanced.PllHeader.Help}" />
                                    </StackPanel>
                                </HeaderedContentControl.Header>
                                <!-- Options stub backlog item 1 (docs/plans/options-stub-item1-pll-tuning-plan.md):
                                     real values now, not placeholders — legacy's own CPLL constructor
                                     defaults (1.0/1/1500/3/900), verified against 3 independent legacy
                                     sites, NOT this block's own former hardcoded guesses (2600/1/200/1200).
                                     IsEnabled bound to IsDemodTypePllSelected — the PENDING radio
                                     selection, matching legacy's own real GBPLL.Enabled gate
                                     (Option.cpp:179-196, RGDemTypeClick calls UpdateUI on every radio
                                     click, not just on Save). OutOrder is a NEW row — legacy's real
                                     dialog has it (pllOutOrder, Option.h:76) but this port's AXAML
                                     never did. Ranges: LoopOrder/OutOrder [1,32] (legacy-faithful —
                                     order 32 = 16 IIR sections, exactly IIRMAX, not an overflow, see
                                     the plan doc's own round-2/3 correction). Cutoffs: static
                                     Maximum=3900 here is UI affordance only (a comfortable ceiling for
                                     the lowest selectable 8000 Hz sample rate's Nyquist) — the REAL
                                     enforcement is PllFmDemodulator.SetTuning's own decoder-side clamp
                                     against whatever sample rate is actually configured. VcoGain
                                     [0.35,10]: 0.35 is the minimum that still covers the full
                                     1500-2300 Hz SSTV tone range inside the capture window; 10 has no
                                     legacy citation, a sane UI ceiling only. -->
                                <Grid ColumnDefinitions="Auto,*,Auto,*" ColumnSpacing="{StaticResource IndustrySpacingSnug}" RowSpacing="{StaticResource IndustrySpacingSnug}" RowDefinitions="Auto,Auto,Auto" IsEnabled="{Binding IsDemodTypePllSelected}">
                                    <TextBlock Grid.Row="0" Grid.Column="0" Classes="IndustryRowLabel" VerticalAlignment="Center" Text="{loc:Translate Options.Advanced.Pll.VcoGain}" />
                                    <NumericUpDown Grid.Row="0" Grid.Column="1" Classes="Industry" Theme="{StaticResource IndustryStepperTheme}" Value="{Binding PllVcoGain}" Increment="0.05" Minimum="0.35" Maximum="10" FormatString="0.00" />
                                    <TextBlock Grid.Row="0" Grid.Column="2" Classes="IndustryRowLabel" VerticalAlignment="Center" Text="{loc:Translate Options.Advanced.Pll.LoopOrder}" />
                                    <NumericUpDown Grid.Row="0" Grid.Column="3" Classes="Industry" Theme="{StaticResource IndustryStepperTheme}" Value="{Binding PllLoopOrder}" Minimum="1" Maximum="32" />
                                    <TextBlock Grid.Row="1" Grid.Column="0" Classes="IndustryRowLabel" VerticalAlignment="Center" Text="{loc:Translate Options.Advanced.Pll.LoopCutoff}" />
                                    <NumericUpDown Grid.Row="1" Grid.Column="1" Classes="Industry" Theme="{StaticResource IndustryStepperTheme}" Value="{Binding PllLoopCutoffHz}" Minimum="1" Maximum="3900" />
                                    <TextBlock Grid.Row="1" Grid.Column="2" Classes="IndustryRowLabel" VerticalAlignment="Center" Text="{loc:Translate Options.Advanced.Pll.OutOrder}" />
                                    <NumericUpDown Grid.Row="1" Grid.Column="3" Classes="Industry" Theme="{StaticResource IndustryStepperTheme}" Value="{Binding PllOutputOrder}" Minimum="1" Maximum="32" />
                                    <TextBlock Grid.Row="2" Grid.Column="0" Classes="IndustryRowLabel" VerticalAlignment="Center" Text="{loc:Translate Options.Advanced.Pll.OutCutoff}" />
                                    <NumericUpDown Grid.Row="2" Grid.Column="1" Classes="Industry" Theme="{StaticResource IndustryStepperTheme}" Value="{Binding PllOutputCutoffHz}" Minimum="1" Maximum="3900" />
                                </Grid>
                            </HeaderedContentControl>

                            <Button Classes="Industry IndustryBtn IndustryBtn24 IndustryBtnSecondary" Padding="7,0" Content="{loc:Translate Options.ResetSection}" Command="{Binding ResetAdvancedToDefaultCommand}" />

                            <HeaderedContentControl Classes="Industry" Theme="{StaticResource IndustryGroupBoxTheme}">
                                <HeaderedContentControl.Header>
                                    <StackPanel Orientation="Horizontal" Spacing="{StaticResource IndustrySpacingTight}">
                                        <TextBlock Classes="IndustryGbTitle" Text="{loc:Translate Options.Advanced.CrossingHeader}" />
                                        <TextBlock Classes="IndustryHelpGlyph" Text="{loc:Translate Options.HelpGlyph}" ToolTip.Tip="{loc:Translate Options.Advanced.CrossingHeader.Help}" />
                                    </StackPanel>
                                </HeaderedContentControl.Header>
                                <!-- Options stub backlog item 2 (docs/plans/options-stub-item2-zerocrossing-tuning-plan.md):
                                     real values now, not placeholders. Outer StackPanel IsEnabled
                                     bound to IsDemodTypeZeroCrossingSelected, matching legacy's real
                                     GBCROSS.Enabled gate (Option.cpp:179-196). RGcrossType 3-way radio
                                     (IIR/FIR/OFF) is a NEW control — legacy has it (Option.dfm's
                                     RGcrossType) but this port's AXAML never did. IIR sub-group
                                     (order+cutoff) and FIR sub-group (smoothing frequency) each grey
                                     further per legacy's real GBCOI/GBCOF.Enabled gate
                                     (Option.cpp:197-212) — shown always rather than replicating
                                     legacy's own hide/show elsewhere, same precedent as the PLL group
                                     above. Smoothing frequency is a free-entry field clamped [500,8000]
                                     (legacy's real two-sided range, Option.cpp:536-540) rather than a
                                     preset dropdown — legacy's own crossSmooz preset list could not be
                                     read from the binary .dfm, see the plan doc's own note. -->
                                <StackPanel Spacing="{StaticResource IndustrySpacingSnug}" IsEnabled="{Binding IsDemodTypeZeroCrossingSelected}">
                                    <StackPanel Orientation="Horizontal" Spacing="{StaticResource IndustrySpacingSnug}">
                                        <RadioButton Classes="Industry IndustryMini" Theme="{StaticResource IndustryMiniRadioTheme}" GroupName="ZeroCrossingSmoothingMode" Content="{loc:Translate Options.Advanced.Crossing.ModeIir}" IsChecked="{Binding IsZeroCrossingSmoothingIirSelected}" />
                                        <RadioButton Classes="Industry IndustryMini" Theme="{StaticResource IndustryMiniRadioTheme}" GroupName="ZeroCrossingSmoothingMode" Content="{loc:Translate Options.Advanced.Crossing.ModeFir}" IsChecked="{Binding IsZeroCrossingSmoothingFirSelected}" />
                                        <RadioButton Classes="Industry IndustryMini" Theme="{StaticResource IndustryMiniRadioTheme}" GroupName="ZeroCrossingSmoothingMode" Content="{loc:Translate Options.Advanced.Crossing.ModeOff}" IsChecked="{Binding IsZeroCrossingSmoothingOffSelected}" />
                                    </StackPanel>
                                    <Grid ColumnDefinitions="Auto,*,Auto,*" ColumnSpacing="{StaticResource IndustrySpacingSnug}" RowSpacing="{StaticResource IndustrySpacingSnug}" RowDefinitions="Auto" IsEnabled="{Binding IsZeroCrossingIirGroupEnabled}">
                                        <TextBlock Grid.Row="0" Grid.Column="0" Classes="IndustryRowLabel" VerticalAlignment="Center" Text="{loc:Translate Options.Advanced.Crossing.Order}" />
                                        <NumericUpDown Grid.Row="0" Grid.Column="1" Classes="Industry" Theme="{StaticResource IndustryStepperTheme}" Value="{Binding ZeroCrossingOutputOrder}" Minimum="1" Maximum="32" />
                                        <TextBlock Grid.Row="0" Grid.Column="2" Classes="IndustryRowLabel" VerticalAlignment="Center" Text="{loc:Translate Options.Advanced.Crossing.Cutoff}" />
                                        <NumericUpDown Grid.Row="0" Grid.Column="3" Classes="Industry" Theme="{StaticResource IndustryStepperTheme}" Value="{Binding ZeroCrossingOutputCutoffHz}" Minimum="1" Maximum="3900" />
                                    </Grid>
                                    <Grid ColumnDefinitions="Auto,*" ColumnSpacing="{StaticResource IndustrySpacingSnug}" IsEnabled="{Binding IsZeroCrossingFirGroupEnabled}">
                                        <TextBlock Grid.Column="0" Classes="IndustryRowLabel" VerticalAlignment="Center" Text="{loc:Translate Options.Advanced.Crossing.Smoothing}" />
                                        <NumericUpDown Grid.Column="1" Classes="Industry" Theme="{StaticResource IndustryStepperTheme}" Value="{Binding ZeroCrossingSmoothingFrequencyHz}" Minimum="500" Maximum="8000" />
                                    </Grid>
                                    <!-- "Differentiator" (sys.m_Differentiator, a picture-channel
                                         edge-enhancement filter) REMOVED 2026-08-28, not wired — see
                                         docs/removed-features.md's own entry. Legacy's own
                                         GetPictureLevelDiff calls GetPixelLevel internally, the exact
                                         same missing decoder-side pixel-correction hook already found
                                         and documented for the polynomial-calibration removal above
                                         in this same tab. -->
                                </StackPanel>
                            </HeaderedContentControl>

                            <HeaderedContentControl Classes="Industry" Theme="{StaticResource IndustryGroupBoxTheme}">
                                <HeaderedContentControl.Header>
                                    <TextBlock Classes="IndustryGbTitle" Text="{loc:Translate Options.Advanced.TxFilterHeader}" />
                                </HeaderedContentControl.Header>
                                <!-- Options stub backlog item 3 (docs/plans/options-stub-item3-tx-bpf-lpf-plan.md):
                                     real values now — a REVERSAL of a documented, deliberate prior
                                     removal (docs/removed-features.md's former "TX output bandpass
                                     filter toggle/tap setting" entry, deleted now that this ships) —
                                     the underlying filter math was already correctly ported and
                                     applied unconditionally; only the toggle/tap control was missing.
                                     TX LPF is genuinely new work, never ported before. Tap count
                                     [2,512] step 2 (even-only, legacy's own MakeFilter leaves one
                                     coefficient slot uninitialized for an odd count — a real UB
                                     divergence this port sidesteps). LPF frequency [100,3000], legacy's
                                     own real Save-handler range (Option.cpp:452-459). -->
                                <StackPanel Spacing="{StaticResource IndustrySpacingSnug}">
                                    <CheckBox Classes="Industry" Theme="{StaticResource IndustryCheckBoxTheme}" Content="{loc:Translate Options.Advanced.TxBpf}"
                                              IsChecked="{Binding TxBpfEnabled}" />
                                    <Grid ColumnDefinitions="Auto,*" ColumnSpacing="{StaticResource IndustrySpacingSnug}" IsEnabled="{Binding TxBpfEnabled}">
                                        <TextBlock Grid.Column="0" Classes="IndustryRowLabel" VerticalAlignment="Center" Text="{loc:Translate Options.Advanced.TxBpf.TapCount}" />
                                        <NumericUpDown Grid.Column="1" Classes="Industry" Theme="{StaticResource IndustryStepperTheme}" Value="{Binding TxBpfTapCount}" Increment="2" Minimum="2" Maximum="512" />
                                    </Grid>
                                    <CheckBox Classes="Industry" Theme="{StaticResource IndustryCheckBoxTheme}" Content="{loc:Translate Options.Advanced.TxLpf}"
                                              IsChecked="{Binding TxLpfEnabled}" />
                                    <Grid ColumnDefinitions="Auto,*" ColumnSpacing="{StaticResource IndustrySpacingSnug}" IsEnabled="{Binding TxLpfEnabled}">
                                        <TextBlock Grid.Column="0" Classes="IndustryRowLabel" VerticalAlignment="Center" Text="{loc:Translate Options.Advanced.TxLpf.FrequencyHz}" />
                                        <NumericUpDown Grid.Column="1" Classes="Industry" Theme="{StaticResource IndustryStepperTheme}" Value="{Binding TxLpfFrequencyHz}" Minimum="100" Maximum="3000" />
                                    </Grid>
                                    <!-- TX sample clock offset stub removed 2026-08-26 (Clock
                                         calibration, stub survey Tier 3, code-review finding): this
                                         was a duplicate, disabled, permanently-showing-0 control for
                                         the SAME setting the Audio tab's own new "TX clock offset"
                                         row now wires for real — see OptionsWindowView.axaml's
                                         Audio-tab section. -->
                                </StackPanel>
                            </HeaderedContentControl>

                            <!-- Options stub backlog items 4 (Loopback mode / RGLoopBack) and 5
                                 (Polynomial demod-level calibration + Level Calibration Wizard):
                                 the whole "Misc"/Diagnostics group is now empty and REMOVED,
                                 2026-08-28 — see docs/removed-features.md's own entries for the full
                                 reasoning (two DIFFERENT root causes, not one shared cause — a
                                 code-review pass caught and corrected an earlier draft of this
                                 comment that wrongly claimed item 5 shared item 4's own blocker).
                                 Item 4 (RGLoopBack): a locked prior architectural decision — this
                                 port's TX/RX are mutually exclusive (capture pauses for a
                                 transmission's whole duration), which real full-duplex TX-while-RX
                                 monitoring needs and this port doesn't support. Item 5 (polynomial
                                 calibration): the wizard itself is actually a pure in-process
                                 software sweep (no hardware, no real TX involved) and would be
                                 buildable — the real blocker is that this port's decoder has no
                                 pixel-level-correction hook of any kind for a calibration table to
                                 plug into at all (legacy's own linear DemOff/DemWhite/DemBlack stage
                                 was never ported either), a genuinely large retrofit out of scope
                                 for an Options-stub fill. -->
                        </StackPanel>
                    </ScrollViewer>
                </TabItem>

                <!-- New tab: real QRZ.com XML Callbook lookup (see QrzCallsignLookup.cs, legacy
                     qrzcom.cpp). NOT named "Logbook": QrzUploadSettings/the QRZ Logbook upload
                     API key has no UI anywhere yet, so a tab titled "Logbook" containing only
                     lookup credentials would misleadingly imply it covers that too. -->
                <TabItem Classes="Industry" Header="{loc:Translate Options.Qrz.Tab}">
                    <ScrollViewer>
                        <StackPanel Spacing="{StaticResource IndustrySpacingGroup}" Margin="{StaticResource IndustryPaddingGroup}">
                            <HeaderedContentControl Classes="Industry" Theme="{StaticResource IndustryGroupBoxTheme}">
                                <HeaderedContentControl.Header>
                                    <TextBlock Classes="IndustryGbTitle" Text="{loc:Translate Options.Qrz.QrzLookupHeader}" />
                                </HeaderedContentControl.Header>
                                <StackPanel Spacing="{StaticResource IndustrySpacingSnug}">
                                    <CheckBox Classes="Industry" Theme="{StaticResource IndustryCheckBoxTheme}" Content="{loc:Translate Options.Qrz.QrzLookupEnabled}"
                                              IsChecked="{Binding QrzLookupEnabled}" />
                                    <TextBlock Classes="IndustryRowLabel" Text="{loc:Translate Options.Qrz.UsernameLabel}" />
                                    <TextBox Classes="Industry IndustryInput" Watermark="{loc:Translate Options.Qrz.QrzLookupUsernameWatermark}"
                                             Text="{Binding QrzLookupUsername}" />
                                    <TextBlock Classes="IndustryRowLabel" Text="{loc:Translate Options.Qrz.PasswordLabel}" />
                                    <TextBox Classes="Industry IndustryInput" Watermark="{loc:Translate Options.Qrz.QrzLookupPasswordWatermark}"
                                             Text="{Binding QrzLookupPassword}" PasswordChar="•" />
                                    <!-- A real QRZ account password (not a scoped/revocable API key like the
                                         separate QRZ Logbook upload feature) is stored in plaintext local
                                         settings: no scoped-credential mode exists for this API, so a hint
                                         is the mitigation, not a fix. -->
                                    <TextBlock Classes="IndustryRowLabel" Text="{loc:Translate Options.Qrz.QrzLookupPasswordHint}" TextWrapping="Wrap" />
                                    <StackPanel Orientation="Horizontal" Spacing="{StaticResource IndustrySpacingTight}">
                                        <Button Classes="Industry IndustryBtn IndustryBtn24 IndustryBtnSecondary" Padding="7,0" Content="{loc:Translate Options.Qrz.TestButton}"
                                                Command="{Binding TestQrzLookupCommand}" />
                                        <TextBlock Classes="IndustryRowLabel" VerticalAlignment="Center" Text="{Binding TestQrzLookupStatus}" />
                                    </StackPanel>
                                </StackPanel>
                            </HeaderedContentControl>

                            <Button Classes="Industry IndustryBtn IndustryBtn24 IndustryBtnSecondary" Padding="7,0" Content="{loc:Translate Options.ResetSection}" Command="{Binding ResetQrzToDefaultCommand}" />
                        </StackPanel>
                    </ScrollViewer>
                </TabItem>

                <TabItem Classes="Industry" Header="{loc:Translate Options.Forwarding.Tab}">
                    <ScrollViewer>
                        <StackPanel Spacing="{StaticResource IndustrySpacingGroup}" Margin="{StaticResource IndustryPaddingGroup}">
                            <HeaderedContentControl Classes="Industry" Theme="{StaticResource IndustryGroupBoxTheme}">
                                <HeaderedContentControl.Header>
                                    <TextBlock Classes="IndustryGbTitle" Text="{loc:Translate Options.Forwarding.Header}" />
                                </HeaderedContentControl.Header>
                                <StackPanel Spacing="{StaticResource IndustrySpacingSnug}">
                                    <TextBlock Classes="IndustryRowLabel" Text="{loc:Translate Options.Forwarding.Description}" TextWrapping="Wrap" />

                                    <TextBlock Classes="IndustryRowLabel" Text="{loc:Translate Options.Forwarding.EmptyHint}"
                                               IsVisible="{Binding !AdifUdpDestinations.Count}" />

                                    <!-- Column-header captions: the Port NumericUpDown's own Watermark doesn't
                                         visibly render in this theme (real-window-verified: no other
                                         NumericUpDown in this codebase relies on Watermark either, they all
                                         use an adjacent label like this instead), so a static header row
                                         above the whole list is the fix, not a per-row hack.

                                         Alignment (auditor code-review finding, round 1: an earlier version
                                         of this header used bare "Auto,*,*,Auto,Auto" columns with no
                                         content in columns 0/4, which auto-sized those columns to 0, since
                                         the row grid below has a REAL CheckBox/Button in those columns,
                                         Auto resolved to two DIFFERENT widths per grid, so the "*" columns
                                         absorbed different amounts of leftover space and every caption from
                                         "Host" onward drifted right of its real field, with "Port" landing
                                         over the Remove button. Avalonia's Grid has no SharedSizeGroup
                                         (that's WPF-only), so the fix is a same-content-shape trick: an
                                         invisible CheckBox/Button in columns 0/4 with the row's own
                                         Classes/Theme/Margin/Padding/Content forces Auto to resolve
                                         IDENTICALLY to the row grid, and Margin="7,0,7,0" on this Grid
                                         compensates for the row's own ContentControl Padding (6) + 1px
                                         border the header has no equivalent of. Verified pixel-exact via a
                                         native-resolution screenshot crop, not just visual inspection:
                                         the earlier version's crop LOOKED plausible while actually being off
                                         by tens of pixels, a real lesson for future header/row alignment
                                         work in this codebase. -->
                                    <Grid ColumnDefinitions="Auto,*,*,Auto,Auto" Margin="7,0,7,0" IsVisible="{Binding !!AdifUdpDestinations.Count}">
                                        <CheckBox Grid.Column="0" Classes="Industry" Theme="{StaticResource IndustryCheckBoxTheme}" Margin="0,0,6,0" Opacity="0" IsHitTestVisible="False" Focusable="False" />
                                        <TextBlock Grid.Column="1" Classes="IndustryRowLabel" Text="{loc:Translate Options.Forwarding.ColumnName}" Margin="0,0,4,0" />
                                        <TextBlock Grid.Column="2" Classes="IndustryRowLabel" Text="{loc:Translate Options.Forwarding.ColumnHost}" Margin="0,0,4,0" />
                                        <TextBlock Grid.Column="3" Classes="IndustryRowLabel" Text="{loc:Translate Options.Forwarding.ColumnPort}" Width="110" Margin="0,0,4,0" />
                                        <Button Grid.Column="4" Classes="Industry IndustryBtn IndustryBtn22 IndustryBtnSecondary" Padding="7,0" Content="{loc:Translate Options.Forwarding.RemoveButton}" Opacity="0" IsHitTestVisible="False" Focusable="False" />
                                    </Grid>

                                    <!-- List-editable destination rows: no mock2 equivalent (real app-added
                                         feature), same "plain bordered, no title notch" box per row as
                                         TxImageEditorPaneView's overlay-element rows. -->
                                    <ItemsControl ItemsSource="{Binding AdifUdpDestinations}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate x:DataType="vm:AdifUdpDestinationRowViewModel">
                                                <ContentControl Classes="Industry" Theme="{StaticResource IndustryBlueprintBorderTheme}" Margin="0,0,0,4" Padding="{StaticResource IndustryGbPaddingNested}" ClipToBounds="False">
                                                    <Grid ColumnDefinitions="Auto,*,*,Auto,Auto" RowDefinitions="Auto" >
                                                        <CheckBox Grid.Column="0" Classes="Industry" Theme="{StaticResource IndustryCheckBoxTheme}"
                                                                  IsChecked="{Binding Enabled}" VerticalAlignment="Center" Margin="0,0,6,0" />
                                                        <TextBox Grid.Column="1" Classes="Industry IndustryInput" Watermark="{loc:Translate Options.Forwarding.NameWatermark}"
                                                                 Text="{Binding Name}" Margin="0,0,4,0" />
                                                        <TextBox Grid.Column="2" Classes="Industry IndustryInput" Watermark="{loc:Translate Options.Forwarding.HostWatermark}"
                                                                 Text="{Binding Host}" Margin="0,0,4,0" />
                                                        <!-- No Watermark here: NumericUpDown doesn't visibly render one in this
                                                             theme (see the column-header Grid above, which is the real fix). -->
                                                        <NumericUpDown Grid.Column="3" Classes="Industry" Theme="{StaticResource IndustryStepperTheme}"
                                                                       Value="{Binding Port}" Minimum="1" Maximum="65535"
                                                                       Width="110" Margin="0,0,4,0" />
                                                        <Button Grid.Column="4" Classes="Industry IndustryBtn IndustryBtn22 IndustryBtnSecondary" Padding="7,0"
                                                                Content="{loc:Translate Options.Forwarding.RemoveButton}"
                                                                Command="{Binding RemoveCommand}"
                                                                CommandParameter="{Binding}" />
                                                    </Grid>
                                                </ContentControl>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>

                                    <Button Classes="Industry IndustryBtn IndustryBtn24 IndustryBtnSecondary" Padding="7,0" Content="{loc:Translate Options.Forwarding.AddButton}"
                                            Command="{Binding AddAdifUdpDestinationCommand}" />
                                </StackPanel>
                            </HeaderedContentControl>

                            <Button Classes="Industry IndustryBtn IndustryBtn24 IndustryBtnSecondary" Padding="7,0" Content="{loc:Translate Options.ResetSection}" Command="{Binding ResetForwardingToDefaultCommand}" />
                        </StackPanel>
                    </ScrollViewer>
                </TabItem>

            </TabControl>

            <!-- Bottom bar: destructive "Reset ALL" on the left with its own inline confirm step
                 (IsConfirmingResetAll), a SaveErrorMessage slot in the middle, Cancel/Apply/Save on
                 the right. Per-section resets above are a single click each (an accidental reset
                 before the next Save/Apply/Connect costs nothing — see ui_transition_plan.md step
                 7's own class-doc-comment update for why this is no longer "nothing is persisted
                 until Save"). -->
            <Grid Grid.Row="1" ColumnDefinitions="Auto,*,Auto,Auto,Auto" Margin="{StaticResource IndustryPaddingGroup}">
                <Button Grid.Column="0" Classes="Industry IndustryBtn IndustryBtn24 IndustryBtnSecondary" Padding="7,0"
                        Content="{loc:Translate Options.ResetAll}"
                        Command="{Binding RequestResetAllCommand}"
                        IsVisible="{Binding !IsConfirmingResetAll}" />
                <StackPanel Grid.Column="0" Orientation="Horizontal" Spacing="{StaticResource IndustrySpacingSnug}" IsVisible="{Binding IsConfirmingResetAll}">
                    <TextBlock Classes="IndustryRowLabel" Text="{loc:Translate Options.ResetAll.ConfirmPrompt}" TextWrapping="Wrap" MaxWidth="260" VerticalAlignment="Center" />
                    <Button Classes="Industry IndustryBtn IndustryBtn24 IndustryBtnPrimary" Padding="7,0" Content="{loc:Translate Options.ResetAll.ConfirmYes}" Command="{Binding ConfirmResetAllCommand}" />
                    <Button Classes="Industry IndustryBtn IndustryBtn24 IndustryBtnSecondary" Padding="7,0" Content="{loc:Translate Options.ResetAll.ConfirmNo}" Command="{Binding CancelResetAllCommand}" />
                </StackPanel>

                <TextBlock Grid.Column="1" Classes="IndustryRowLabel" Foreground="{DynamicResource IndustryDanger}" Text="{Binding SaveErrorMessage}" TextWrapping="Wrap" VerticalAlignment="Center" Margin="8,0" IsVisible="{Binding SaveErrorMessage, Converter={x:Static ObjectConverters.IsNotNull}}" />

                <Button Grid.Column="2" Classes="Industry IndustryBtn IndustryBtn24 IndustryBtnSecondary" Padding="7,0" Content="{loc:Translate Options.Cancel}" Command="{Binding CancelCommand}" ToolTip.Tip="{loc:Translate Options.Cancel.Help}" Margin="0,0,4,0" IsVisible="{Binding !IsConfirmingResetAll}" />
                <Button Grid.Column="3" Classes="Industry IndustryBtn IndustryBtn24 IndustryBtnSecondary" Padding="7,0" Content="{loc:Translate Options.Apply}" Command="{Binding ApplyCommand}" ToolTip.Tip="{loc:Translate Options.Apply.Help}" Margin="0,0,4,0" IsVisible="{Binding !IsConfirmingResetAll}" />
                <Button Grid.Column="4" Classes="Industry IndustryBtn IndustryBtn24 IndustryBtnPrimary" Padding="7,0" Content="{loc:Translate Options.Save}" Command="{Binding SaveCommand}" IsVisible="{Binding !IsConfirmingResetAll}" />
            </Grid>

        </Grid>
    </Border>

</Window>
