# UI

## Related

[[01-architecture]] (layering rule: UI never touches radio drivers directly) · consumes → `ScanlineStudio.Application` service interfaces from every other module · replaces `Main.cpp`/`Main.dfm` and the ~30 dialog `.cpp`/`.dfm` pairs (`Option`, `RadioSet`, `LogSet`, `MacroKey`, `ColorSet`, etc.)

## Purpose

Define the presentation layer: window/dialog structure, MVVM conventions, and how the UI stays fully decoupled from hardware and DSP internals per the CLAUDE.md rule "UI must never directly communicate with radio drivers."

## Framework and pattern

Avalonia UI 11, Fluent theme, strict MVVM via CommunityToolkit.Mvvm. Every view (`.axaml`) has exactly one view-model (`ObservableObject` or `ObservableRecipient`). **Overstated claim corrected**: this used to say views contain no code-behind logic beyond `InitializeComponent()` and purely visual concerns — not true in practice for every view. Most views' code-behind really is that thin (`RxImagePaneView`/`TxControlsPaneView`/`WaterfallPaneView`/`RadioHeaderView` are all ~10 lines), but `MainWindow.axaml.cs` (~914 lines, grown from an earlier ~309: window-position save/restore, opening Options/About/QsoLink/MacrosReference/FavouritesEditor/ToneGenerator/LoopbackSelfTestResult/RadioConnectionGaveUp child windows, a configurations quick-switch menu, exit handling) and `TxImageEditorPaneView.axaml.cs` (~807 lines: the interactive crop/resize/drag-handle canvas logic) both carry real, non-visual logic in code-behind by deliberate choice — direct window-lifecycle/dialog-hosting concerns and low-level pointer-event canvas interaction are two cases this codebase puts in code-behind rather than forcing through a view-model, not an accident or a rule violation. View-models depend only on `ScanlineStudio.Application` service interfaces, injected via constructor — never on `ScanlineStudio.Core.*` concrete types directly, which is the structural mechanism enforcing the layering rule from [[01-architecture]].

```csharp
public sealed partial class MainWindowViewModel : ObservableObject
{
    private readonly IRadioSessionService _radio;      // ScanlineStudio.Application, not ScanlineStudio.Core.Radio
    private readonly ISstvSessionService _sstv;
    private readonly ILogbookService _logbook;

    [ObservableProperty] private RadioStateViewModel? _currentRadioState;
    [ObservableProperty] private ImageViewModel? _receivedImage;

    public MainWindowViewModel(IRadioSessionService radio, ISstvSessionService sstv, ILogbookService logbook)
    {
        _radio = radio; _sstv = sstv; _logbook = logbook;
        _radio.StateChanges.Subscribe(s => CurrentRadioState = RadioStateViewModel.From(s));
    }
}
```

## Visual design direction

**The Industry design language (current, 2026-08-11)**: a technical "blueprint wireframe" look —
steel-blue accent (`#5980A6`) on a light, flat ground (`#F2F2F3`), square corners everywhere,
hairline (1px) borders instead of filled card backgrounds, `Barlow`/`Barlow Condensed` for
headings and body text, a bundled monospace (`DejaVu Sans Mono`) for every numeric/data readout,
and small "blueprint" corner-mark crosses on select bordered containers (group boxes, plot
frames) as the one deliberate decorative flourish. Built from a user-provided mockup
(`mockups/guidance/SSTV Console.dc.html` + its design-system CSS, `mockups/guidance/
LAYOUT-SPEC.md` for exact pixel/color/spacing measurements) and ported view-by-view across an
8-phase plan (fonts/tokens → atoms → chrome → Receive/Transmit/Gallery/Logbook tabs → Options
window → cleanup). Reference: a wireframe/schematic instrumentation aesthetic, not a specific
existing ham-radio app.

**This supersedes the previous "Aesthetic Directive"** (flat WSJT-X/fldigi-style utilitarian
chrome — gray Win32/Qt control faces, sunken monospace readout fields, filled card backgrounds,
`mockups/MainWindow_mock2.axaml`) in full. That directive's own implementation
(`Styles/Tokens.axaml`/`Cards.axaml`/`ChromeOverrides.axaml`) has been **deleted** — every view
now draws exclusively from `Styles/AtomsTokens.axaml` (color/spacing/font tokens) and
`Styles/Atoms.axaml` (the reusable control atom set: group boxes, chips, steppers, sliders,
segmented controls, tables, thumbnails, buttons, checkboxes, radios, etc. — see that file's own
numbered sections for the full inventory). The prior directive's borderless "raw SDR-
instrumentation" look (cuSDR64/Perseus/SDR++ reference) and its brief WSJT-X/fldigi-card
successor are both now historical only; neither is drawn from anywhere in the app.

**Color, specifically (2026-08-14, `mockups/colors/SSTV-Color-Scheme.md`)**: a rule, not just a
palette — grey is the default, and exactly four hues are reserved, each with one job: steel blue
`#0F62A8` (selection/focus/progress), green `#2E9E44` (healthy activity), amber `#C88A00`
(attention, nothing broken yet), red `#B42A2A` (radiating or destroying). A hue never does two
jobs; saturated color only ever appears in small fills (an LED, a lozenge, a solid button), never
a panel/card/bar background. This is the standing reference for how ANY control — existing or
new — gets colored: buttons (grey until it commits or destroys), fields (background states what
you may do to it: white=editable, sunken grey=read-only data, dimmed=disabled), selectors (blue
marks what's chosen, never green/amber/red), and status indicators (LED+lozenge, the only place
saturated color sits at rest). Full rules, hex values, and the fixed-exception list (HALT TX
always red, TX/RX LED pair by ham convention, spectrum/waterfall's own dark instrument palette,
image thumbnails uncolored) are in that file — read it before adding any new colored element,
button class, or status indicator; don't improvise a color choice.

**Explicitly banned** (unchanged across all three directives): rounded corners (`CornerRadius`
must be `0` everywhere — no exceptions); gradients; drop shadows; glowing backgrounds; excessive
whitespace; center-aligning layouts arbitrarily just to fill space.

**Two implementation gotchas migrated from session notes, not obvious from the code alone**:
`MainWindow.axaml`'s root `Window` needs an explicit `Background="{StaticResource IndustryBg}"` —
it silently relied on the old (now-deleted) `Cards.axaml`'s bare `Window` selector for this before,
which broke (whole page went pure white) the moment that file was removed; if a future full-app
background change is ever needed, this is the one place to change it, most of the page relies on
this root fill showing through uncovered areas. **Stale as of 2026-08-17**: the Transmit tab's
centre column (TX Image Editor) used to be correctly blank until an image was selected
(`MainViewModel.ActiveEditor` null-until-populated by design) — this was superseded by a user
decision that the editor should always be open by default, landing on a blank placeholder editor
immediately rather than requiring a manual "Open editor" click; `ActiveEditor` is populated at
construction now. **Citation corrected**: not `TxControlsPaneViewModel`'s own constructor —
`MainViewModel`'s constructor wires `txControls.EditorOpened`/`EditorClosed` to set/clear
`ActiveEditor`, then fire-and-forget invokes `txControls.OpenBlankEditorCommand.ExecuteAsync(null)`
(via `OpenBlankEditorSafelyAsync`, with its own try/catch — a Tier B audit finding fixed a case
where that catch didn't cover everything `OpenBlankEditorCommand.ExecuteAsync`'s own try/catch
could still let through). If the column shows blank/empty with no editor chrome at all, THAT is the
bug now, not the reverse.

**Layout & density rules** (unchanged in spirit, now expressed via the Industry token scale):
- **Extreme information density.** Screen real estate is at an absolute premium — tightly clustered
  controls, not spaced-out ones.
- **Minimal padding.** The Industry spacing scale (`AtomsTokens.axaml`'s `IndustrySpacing*`/
  `IndustryPadding*` resources) runs 2px (`Hairline`) to 11px (`Group`); 2-4px is the default for
  control-to-control spacing, larger values reserved for outer card/group padding, matching
  `mockups/guidance/PADDING-NORMALIZATION.md`'s derived table.
- **Grid-first geometry.** Rigid `Grid` layouts with precise row/column definitions, not loose flowing
  `StackPanel`/flex-box-style layouts for structural layout (a `StackPanel` for a short run of tightly
  packed controls inside one grid cell is fine; using it as the top-level layout mechanism is not).
  Reserve generous grid cells for raw data visualization (FFT spectrum, waterfall), but **not** at
  the expense of the main window's actual primary purpose — direct user correction, after seeing it
  running: "the main window should be for images, receiving, editing, tx-ing," not the waterfall.

**Typography & controls**:
- **Monospace for all data.** Frequency readouts, signal meters, numeric inputs, and any rapid-update
  display **must** use the bundled monospace font (`IndustryMonoFontFamily`, `DejaVu Sans Mono`) —
  prevents layout jitter on fast redraws. Unchanged in spirit from every prior directive.
- **Utilitarian standard text.** Labels/button text: small, crisp, legible (`Barlow`/`Barlow
  Condensed`, 9.5-12.5px depending on control). No oversized "eyebrow" text, no large headers —
  this is instrumentation, not marketing, with one narrow exception: small accent-colored
  monospace "kicker" labels (`IndustryKicker`) for section eyebrows, matching the mockup's own
  wireframe-schematic convention.
- **Hairline-bordered, strictly rectangular controls with explicit state feedback.** Buttons/
  toggles/chips show hover/pressed/checked/disabled state via border and fill swaps against a
  small fixed accent ramp (`IndustryAccent`/`600`/`700`/`800` + a handful of literal hover-mix
  tints), never a rounded "pill" — square corners and 1px hairline borders are the one
  least-negotiable rule carried through all three directives.

Custom-drawn controls (waterfall, spectrum trace) still carry the most visual weight and are
worth real design attention beyond a default `Canvas` fill. The waterfall's own color gradient is
a deliberate, previously-audited departure from strict Industry-palette fidelity (a 6-stop
SDR-style heatmap — WSJT-X/SDR++/GQRX convention — not the mockup's own flat single-tone plot;
see [[14-roadmap]]'s waterfall color/palette item and `WaterfallPalette.cs`), while the
surrounding chrome (plot frame border, controls, tabs, status fields) follows the same Industry
atoms as everywhere else.

## Window/dialog inventory (mapped from legacy)

**Corrected against the shipped fixed-tab shell** — this table originally assumed a dockable-pane
main window with separate `LogbookView`/`MacroKeyEditor`/`PluginManagerDialog` windows; several of
those never shipped under those names because the whole navigation shell changed (see "Main window
layout" below). `AboutWindowView` and `QsoLinkWindowView` — both real, shipped windows — were
missing from this table entirely.

| New view | Legacy equivalent | Notes |
|---|---|---|
| `MainWindow` | `Main.cpp`/`Main.dfm` | Fixed shell: menu/header cards + a Receive/Transmit/Gallery/Logbook `TabControl` (see "Main window layout" below) — waterfall, RX image, TX controls, and the interactive frequency/mode status strip (frequency+mode control, memory presets, a real WSJT-X-style RX level meter, and Pwr — renamed from "TX volume" 2026-08-24, app-internal playback gain, not OS device volume) are each panes inside those tabs, not separately docked. The header strip's own Tune is real backend (`ISstvSessionService.TuneAsync`) but has no header control bound to it; a working Tune UI now lives on the Options window's own Radio/CAT tab instead (see that row below), a separate `OptionsWindowViewModel`-owned implementation. |
| `OptionsWindow` (tabbed) | `Option.cpp`, `RadioSet.cpp`, part of `ColorSet.cpp` | **Built**, and grown well past the originally-planned General/Audio/Radio/TX 4 tabs — 9 tabs today: General, Audio, Radio, Station (renamed from "TX" 2026-08-24), Decode, Identification, Advanced, QRZ, Forwarding. One unified dialog, not legacy's separate per-topic popups (design decision, see the Settings/Options plan): General folds in what would have been a separate `LanguageSettingsDialog` (runtime culture switch, see [[10-localization]]); Radio folds in what would have been `RadioSettingsDialog` (backend selection — None/rigctld/Hamlib/flrig/OmniRig (OmniRig Windows-only, gated by `IsOmniRigBackendAvailable`) — connection fields, plus a Hamlib library auto-detect/browse/test path and a Pwr+Tune card for dialing in TX power against the radio's own meter, see [[02-radio-layer]]/[[04-rigctld]]/[[03-cat-layer]]). A Display tab (waterfall/scope palette, folding in `ColorSet.cpp`/`ColorBar.cpp`) is still **not built**, and no `WaterfallPaletteSettings` section exists — but this is now a deliberate, re-confirmed decision, not an open gap: 2026-08-09 batches 8a/8b gave the waterfall/spectrum real color rendering (a fixed 6-stop heatmap, not legacy's flat 2-color, see [[14-roadmap]]'s waterfall color/palette item) without adding per-element user customization — the 7 disabled `OptionsWindowView.axaml` color-picker buttons this row's own `ColorSet.cpp`/`ColorBar.cpp` citation refers to stay intentionally disabled (their own help text already documents this: one fixed palette, not per-element pickers). |
| TX image editor (in-window view, not a dialog) | `PicRectDlg.cpp`/`PicRect.cpp` + `TextIn.cpp`/`TextEdit.cpp` | Combined crop/resize/stretch, adjustments, and the [[15-template-designer]] template compositor (superseding the "text overlay" scope this row originally described), realtime preview, keyboard-nudge precision — see [[07-image-pipeline]]'s "TX image editor" section. Macro-key text substitution (`MacroKey.cpp`'s equivalent) is inline here too: an element's free-typed text is resolved through `IMacroTextResolver` — no separate `MacroKeyEditor` window exists or is planned. |
| `RxHistoryPane` (in the Gallery tab, not a dockable pane or a dialog) | `HistView.cpp` + `Main.h`'s `TabHist` page | RX history browser, see [[07-image-pipeline]] |
| Stock/template picker (inline in `TxControlsPaneViewModel`, not a dialog) | `StockVew.cpp` | See [[07-image-pipeline]] |
| Logbook (a `MainWindow` tab, not a separate window) | `LogList.cpp`, `Qsodlg.cpp` | See [[08-logging]] |
| `QsoLinkWindowView` | n/a | **Built, missing from this table before.** Links a Gallery/RX-history entry to a logged QSO (`IReceiveHistoryStore.SetLinkedQsoIdAsync`, [[07-image-pipeline]]'s "RX history" section) — no direct legacy equivalent. |
| `AboutWindowView` | n/a | **Built, missing from this table before.** Version/license info. |
| `RadioConnectionGaveUpWindowView` | n/a | **Built, missing from this table before.** Must-acknowledge popup shown after 5 failed radio connection attempts (replaced an earlier toast, see [[02-radio-layer]]'s polling section); a Config shortcut opens `OptionsWindow` directly to the Radio tab. |
| `MacrosReferenceWindowView` | n/a | **Built, missing from this table before.** Read-only reference listing every `IMacroTextResolver` token (`%m`/`%D`/`%T`, `{name}`/`{grid}`/etc. — see [[07-image-pipeline]]'s "Overlay text specifics"), opened from `MainWindow`. |
| `FavouritesEditorWindowView` | n/a | **Built, missing from this table before.** Editor for saved frequency/mode favourites. |
| `ToneGeneratorWindowView` | n/a | **Built, missing from this table before.** Standalone audio test-tone generator dialog. |
| `LoopbackSelfTestResultWindowView` | n/a | **Built, missing from this table before.** Result display for `ISstvSessionService.RunLoopbackSelfTestAsync`'s one-shot software encode→decode self-test (see [docs/removed-features.md](../docs/removed-features.md)). |
| `PluginManagerDialog` | n/a | Not built — see [[11-plugin-system]] (plugin host itself is scaffolded, empty). |

Not every legacy dialog needs a 1:1 rebuild on day one — [[14-roadmap]] sequences these; the table above is the target inventory, not a v1 requirement list.

## Main window layout

Legacy's actual main window is **paged**, not simultaneous: `Main.h` declares a real `TPageControl
*Page` with `TabSync`/`TabRX`/`TabHist`/`TabTX`/`TabTemp` tab sheets (`ComLib.h`'s
`enum { pgSync, pgRX, pgHist, pgTX, pgTemp }`), switched one-at-a-time via `Mmsstv->AdjustPage(...)`
— this corrects an earlier draft of this section that incorrectly claimed no legacy tab mechanism
existed at all (a `.dfm`-only grep miss — `.dfm` files are binary and were the only thing checked;
`Main.h`'s plain-text component declarations were not).

**Superseded — this section originally described a `Dock.Avalonia`-based dockable-pane layout (a
fixed waterfall strip, an RX Image/RX History tab group, a separate TX Controls region, all
simultaneously visible and resizable/tear-outable) that was tried, then fully replaced.** The
shipped design ([[07-image-pipeline]]'s "Navigation" section has the same correction) is a **fixed
shell**: a menu/header-card row plus a `TabControl` with 4 source-ordered tabs — Receive (0),
Transmit (1), Gallery (2), Logbook (3) — bound two-way to `MainViewModel.SelectedTabIndex`
(`MainWindow.axaml`). Each tab is its own fixed multi-column `Grid`, not a further dockable region:
- **Receive**: waterfall (a fixed strip, not tab-grouped with anything — kept from the dockable
  design's own lesson that a tab-grouped waterfall took over its region when active, which no real
  SDR-instrument reference this project draws from — SDR++/cuSDR64/Perseus — actually does), RX
  image, frame metadata/unattended-RX cards.
- **Transmit**: a 2-column grid — left column `TxControlsPaneView` (Mode/Identification/Output/
  Stock cards), center column the TX image editor (`MainViewModel.ActiveEditor`, swapped via a
  nullable `ContentControl.Content` binding), now taking the full remaining width. **Was a 3-column
  grid** with a right-hand queue/mode-timing/TX-log/recently-sent column until 2026-08-25, when the
  queue/TX-log/recently-sent cards were removed (no such feature exists, "maybe one day" — see
  `spec/14-roadmap.md`) and the mode-timing card was removed by direct user instruction to give the
  editor its width back.
- **Gallery**: RX history (`RxHistoryPane`/`RxHistoryPaneViewModel`, still those names in code) plus
  a Storage card, filterable by mode/today-only/flagged.
- **Logbook**: the QSO log view, [[08-logging]].

This is still a deliberate modernization past legacy's one-view-at-a-time paging (RX/TX/Gallery are
each reachable without leaving the tab entirely blank the way legacy's paging did), just achieved
with a different, simpler mechanism than originally planned — see [[07-image-pipeline]]'s
"Navigation" section for the fuller history of why the dockable-pane design was abandoned. Legacy's
former dialog-based `HistoryView`/`StockImageView` design (an even earlier iteration than the
dockable-pane one) was never built either way. Layout is implemented with Avalonia's `Grid`/
`DockPanel`, not a fixed-pixel canvas, so it can be resized/DPI-scaled cleanly, which the legacy VCL
fixed-layout forms could not do well; `Dock.Avalonia` itself is not a package reference of
`ScanlineStudio.UI.csproj` today.

**Tab visibility / layout persistence**: unlike the dockable-pane design's "closing a tool pane has
no way back" problem this section used to warn about, a fixed tab can't be closed at all — there is
nothing analogous to build a `View` menu against, and that plan is moot. Layout persistence across
restarts is still not built: [[12-settings]] has no "last active tab" field, so every session starts
on the Receive tab (`SelectedTabIndex`'s default) regardless of which tab was open when the app last
closed — the same accepted regression against legacy's `Main.cpp` `ReadInteger("Window","MainPage",
pgRX)` this section flagged before, just under the new shell rather than the dockable one.

## Custom rendering

Waterfall and oscilloscope views are custom `Control`-derived Avalonia controls rendering via `DrawingContext` (or `SkiaSharp` custom draw if frame-rate profiling shows `DrawingContext` insufficient), fed by [[06-sstv-dsp]]'s `IWaterfallSource` — the view-model exposes only the already-computed magnitude-frame data; no FFT or filter math lives in `ScanlineStudio.UI`.

## Porting gotchas: reading `.dfm` files as reference

Legacy `.dfm` files are **binary**, not text (`Main.dfm` opens with a `FF 0A 00` binary-DFM header), and several contain embedded assets inline — `Main.dfm` has at least one bitmap (`BM` header) embedded mid-file, meaning some button/icon glyphs live *inside* the form resource, not only in `res/`. Treat `.dfm` files as needing a proper DFM-to-text conversion step (e.g. via a Delphi/C++Builder toolchain, or a third-party DFM parser) before anyone tries to "read the layout off the file" — grepping the raw bytes will miss both structure and embedded assets silently.

Even once converted to text, the coordinate/sizing values are **VCL-specific and not portable as numbers**: `Font.Height` is stored negative (pixel height, not point size), forms carry `PixelsPerInch = 96` plus `Scaled`/`DesignSize` properties that interact with VCL's own DPI-scaling model. Copying these numbers directly into Avalonia `Grid`/`DockPanel` layouts will misplace controls, because Avalonia's default font metrics and DPI model don't match VCL's 1:1. Use `.dfm` files as a reference for *which controls exist and their rough relative arrangement*, not as a source of exact pixel values to transcribe.

## Theming

Light/dark theme support using Avalonia's `FluentTheme` variants, user-overridable (settings UI not built yet) — a straightforward modernization over the legacy fixed VCL look, in line with the "Modern UI" design goal in [[00-project-overview]]. **Defaults to Light explicitly**, not OS-preference-follow (`App.axaml`'s `RequestedThemeVariant="Light"`) — a direct user correction after seeing the app default to dark on a dark-OS-theme machine; supersedes an earlier "follow OS preference by default" draft of this line.

## Testing

View-models are tested headlessly (no window shown) via Avalonia's headless test platform + xUnit: construct a view-model with faked `ScanlineStudio.Application` service interfaces, drive property/command changes, assert observable state — see [[13-testing]] for the full strategy. Visual/layout regressions are out of scope for automated testing and rely on manual verification per release.

## Definition of done

- [x] `MainWindow` walking skeleton (referenced in [[01-architecture]]) renders with live waterfall fed by [[05-audio-engine]]/[[06-sstv-dsp]] — proven against real hardware, not just fixtures (Phase 3's end-to-end demo).
- [x] **Stale premise, updated**: this line used to say only `MainWindow` was built and the rest were phase-bucketed with no per-item status. That's no longer true — every row in the inventory table above except `PluginManagerDialog` is now built ([[14-roadmap]] has the phase-by-phase history in git/`PROJECT_BRIEF.md`, not backfilled per-dialog here). `PluginManagerDialog` remains the one open item, tracked against [[11-plugin-system]]'s own still-scaffolded plugin host.
- [x] No `ScanlineStudio.UI` project reference to `ScanlineStudio.Core.Radio.*`, `ScanlineStudio.Core.Audio`, or `ScanlineStudio.Core.Sstv` concrete assemblies — verified by a hand-rolled reflection + `.csproj`-parsing architecture test (`UiLayeringArchitectureTests`), not `NetArchTest` (avoided pulling in its `Mono.Cecil` dependency for one rule).
- [x] `UiLayeringArchitectureTests` extended to also assert `ScanlineStudio.UI.csproj` declares no `PackageReference` to `Microsoft.Data.Sqlite` or `SixLabors.ImageSharp` — done, via a `bannedPackagePrefixes` prefix match (`"Microsoft.Data.Sqlite"`, `"SixLabors."`) rather than the originally-planned exact-name check, so `SixLabors.ImageSharp.Drawing`/`SixLabors.Fonts` (needed for [[07-image-pipeline]]'s `ApplyTemplate` text rendering) are caught too, not just the two package names this line originally named.
